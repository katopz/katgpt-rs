[package]
name = "katgpt-kv"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "KV-cache namespace — KV-cache compression, compaction, projection-sharing, and quantization backends. Spun out of katgpt-rs/src/{kv_share,osc_kv,cs_kv_probe,shard_kv,sp_kv,still_kv,kvarn,targeted_precision} (Issue 015 Phase 3)."
repository = "https://github.com/katopz/katgpt-rs"
keywords = ["kv-cache", "quantization", "inference", "modelless", "compression"]
categories = ["algorithms", "science"]
publish = false  # only katgpt-core ships to crates.io

[dependencies]
# SIMD kernels, types re-export, QuantizedKVCache trait (via types).
katgpt-core = { path = "../katgpt-core" }
# Shared types — QuantizedKVCache trait lives here (Issue 015 Phase 1).
katgpt-types = { path = "../katgpt-types" }
# Spectral quantization substrate — used by shard_kv (K-path PCA + Lloyd-Max)
# and kvarn (via targeted_precision). Required, non-optional.
katgpt-spectral = { path = "../katgpt-spectral" }
# f16 KV cache storage (still_kv, shard_kv).
half = "2"
# Zero-copy Pod layout for KV structs.
bytemuck = "1"
# KV cache parallel dequant kernels.
rayon = "1.10"
# serde for KV config structs.
serde = { version = "1", features = ["derive"] }
# Deterministic RNG (cs_kv_probe).
fastrand = "2"
# Hashing (cs_kv_probe masks).
blake3 = "1"

[features]
# All flags mirror the historical katgpt-rs feature surface for KV modules.
# Empty tracking flags — they gate `#[cfg(feature = "...")]` branches inside
# the code, not external deps. The root crate forwards each one via
# `feature = ["katgpt-kv/feature"]`.
default = []

# Q-K=V projection sharing — 50% KV cache reduction (Plan 185).
kv_share = []

# Oscillatory KV cache with IMEX discretization (Plan 189 Phase 2).
osc_kv = []

# CS-KV-Importance Probe + Density-Budget Interpolator (Plan 280).
cs_kv_probe = []

# ShardKV asymmetric K/V compression (Plan 147, Research 109).
# Requires spectral_quant + turboquant from katgpt-spectral — the root
# crate's `shard_kv` feature adds both, so callers enabling katgpt-kv's
# shard_kv directly MUST also enable katgpt-spectral/spectral_quant +
# katgpt-spectral/turboquant. Documented at the call site.
shard_kv = []

# SP-KV self-pruned key-value attention (Plan 070). Issue 727 (2026-09-05):
# the gate-bias path has a MEASURED per-position load cost of ~+7..12% vs
# NoBias at hd=4 (roughly scale-invariant in t_n; the paper's <1% was a
# t_n=16 artifact and the <3% bar is NOT met — known cost, not a re-pin).
# The T2 hoist makes pruned positions ~free (prune-skip 1.12..1.58x at 48%
# pruned, bar >1.05x met) and is bit-identical to the pre-hoist math.
sp_kv = []

# StillKV perceiver-based KV cache compaction (Plan 245).
still_kv = []

# KVarN variance-normalized KV-cache quantization (Research 159).
# Requires spectral substrate transitively via targeted_precision.
kvarn = []

# Issue 883 P1 (Research 587): forwards katgpt-core's fitted_value_tables so
# the token-mean-removed V-quant decorator can be gated against the REAL
# KVarN backend (Bench 895 G1/G2/G3/G4 — tests/bench_895_mean_removed_v_quant_goat.rs).
fitted_value_tables = ["katgpt-core/fitted_value_tables"]

# Targeted Precision Budget — per-head bit allocation (Plan 227 Phase 2).
# Used only by kvarn; promoted to top-level module here for visibility.
targeted_precision = []

# ── Phase 5 absorption (Proposal 003, 2026-07-04) ─────────────────────────────
# Moved from `katgpt-rs/src/{cache_prune, segment_checkpoint, async_qdq.rs}`.
# Root re-export preserves `katgpt_rs::{cache_prune, segment_checkpoint,
# async_qdq}` paths via `pub use katgpt_kv::X`.

# CachePrune — SAT + rolling hash + sensitivity masking (Plan 140, Research 101).
# Self-contained: no katgpt-rs internal deps.
cache_prune = []

# Segment Checkpoint — GRM segment caching (Plan 223b). DEFAULT-ON in root.
segment_checkpoint = []

# DriftSegmentStore — training-free drift-segmented multi-state memory
# (Issue 652 / Research 482, arXiv:2606.10650 modelless adaptation).
# Composes temporal_deriv (drift score) × segment_checkpoint (bounded slots)
# × hope_compactor's pair-merge pattern (adjacent-density merge).
# Opt-in — bench_635 GOAT PASS (2026-08-15); consumers landed (riir-ai
# npc_episodic, neuron-db wake-merge policy) — promotion candidate at next re-gate.
drift_segment = []

# SSC Sparse Speculative Drafting — opt-in sub-module of segment_checkpoint
# (Plan 223c). Gates `pub mod ssc;` in segment_checkpoint/mod.rs. Root forwards
# this from the `ssc_spec_draft` feature; the example
# `segment_checkpoint_demo` requires it for the ssc branch.
ssc_spec_draft = ["segment_checkpoint"]

# Async Q/DQ Overlap — double-buffered KV dequantize for GPU pipeline
# (Plan 227 Phase 6). The `inference_router` dep stays in the ROOT feature
# forward (`async_qdq_overlap = ["katgpt-kv/async_qdq_overlap", "inference_router"]`)
# because it gates the GPU backend test harness, not the module itself.
async_qdq_overlap = []

# RadixAttention radix-tree prefix index (Issue 771) — chunk-granular
# longest-prefix matching over a paged KV pool, leaf-preferential LRU with
# lock-aware eviction. Pure index: owns page INDICES, never buffers (CUDA-
# graph address stability by construction). The pool seam lives on
# `katgpt_transformer::PagedKVCache` (chunk_page_tables / retain / release /
# adopt). Node arena re-pointed onto katgpt-core's GraphStablePool (Issue
# 800 C1 — the extracted form of this tree's inline free-list contract).
# Opt-in — GOAT bench_762; promotion candidate when a multi-request
# serving lane consumes it.
radix_prefix_cache = ["katgpt-core/graph_stable_pool"]

# Plan 583 T3.5 — offline MI quant-fidelity probe (audit-only): forwards
# katgpt-core's `mi_est` so the I(W; Ŵ) probe measures retained information
# through the shipped KVarN round-trip alongside the reconstruction metrics.
# Implies `kvarn` (the probed surface). Test-support only — no production
# consumer, never a default.
mi_probe = ["kvarn", "katgpt-core/mi_est"]

[[test]]
name = "bench_694_mi_quant_fidelity_probe"
required-features = ["mi_probe"]

[[test]]
name = "radix_prefix_cache_tests"
required-features = ["radix_prefix_cache"]

# Issue 883 P1 — token-mean-removed V quant over the KVarN backend GOAT:
# G1 dashboard-predicted vs measured quant-MSE drop (+ absmax caveat + sink
# arms), G2 fused dequant+restore paired A/B, G3 miss / E=0 bit-identical,
# G4 0 extra allocs.
[[test]]
name = "bench_895_mean_removed_v_quant_goat"
harness = false
required-features = ["kvarn", "fitted_value_tables"]

# Issue 894 — KVarN dequant kernels as bounds-check-free zips: G3 old-vs-new
# bitwise (the pre-894 loops kept verbatim in tests/common/kvarn_dequant_oracle.rs),
# G4 0 allocs, G2 paired interleaved new/old on the plain dequant+axpy pass.
[[test]]
name = "bench_894_kvarn_dequant_zip_goat"
harness = false
required-features = ["kvarn"]

# Issue 800 Arm B1 — lock-free slot-flip PoC bench. harness = false: the PoC
# prints its own results table + the B2 decision-gate verdict (it is not a
# criterion bench). Standalone: uses only std, no new deps, and does not
# touch src/async_qdq.rs (the upgrade is decision-gated as a follow-up).
[[bench]]
name = "slot_flip_poc"
harness = false
