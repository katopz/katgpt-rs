# Pins for scripts/instrument_reachability_drift_sweep.py — EVERY contract repo.
#
# Format, 4 whitespace-separated fields:
#
#   <repo> <min_scripts> <min_roots> <max_unreachable>
#
# Re-pin DELIBERATELY, in the commit that changes the measured value, with the
# reason in the commit message.
#
# ── max_unreachable is a RATCHET, and ONLY here ─────────────────────────────
# katgpt-rs's own unreachable set is pinned by MEMBERSHIP, with a REASON per
# row, in scripts/instrument_unreferenced_expected.txt — that is the per-push
# gate, over the repo that owns the rows.
#
# Workspace-wide the same predicate finds far more, and riir-train is all of
# them: its scripts/ is almost entirely plan-scoped one-offs
# (plan341_band_pool.py, plan346_diversity_gate.py, t504_harvest.py) and its
# AGENTS.md names none of them. In that repo the predicate OVER-CAPTURES — a
# plan artifact is not an instrument, and "unfindable from AGENTS.md" is the
# correct state for a script whose whole life was one plan task.
#
# So the ceiling here constrains the DERIVATIVE: the commit that adds ANOTHER
# unfindable script reds, and the existing rows are their own repos' to
# adjudicate. That is the strongest claim this repo can honestly make about
# somebody else's tree.
#
# ⛔ The counts are DERIVED and printed by the sweep's own summary line; do not
# restate one here. The version of this header that did went stale the day a
# sibling repo's tooling was documented, and the sweep printed a figure that
# contradicted the measured total one line above it.
#
# ⚠ This is deliberately NOT the Issue 785 rule's target. That rule forbids
# ratcheting a bucket meaning *unanswered* — a backlog with no owner. This
# bucket means *unfindable*, every row has an owner, and the action on a red is
# immediate and local: name it, invoke it from something named, or decide it is
# ephemeral. It is also not the suite_membership_audit outcome (1,203 rows,
# report-only, no verdict at all): there no per-commit action follows from the
# number.
#
# ⛔ Do not "fix" a red by raising a ceiling without reading the row. The class
# this catches is measured: len_derived_binding_audit.py sat unreachable — 983
# lines, cross-repo, findings walled at 0, named only in HISTORY.md — through
# TWO censuses that were looking for exactly it (Issues 785, 786).
#
# ── the two floors, and where neither bites ────────────────────────────────
#   min_scripts   moves when the WALK goes blind
#   min_roots     the PERMISSIVE direction: with fewer roots MORE scripts read
#                 as unreachable, so a shrinking root set inflates the count
#                 rather than hiding it — this floor is about the instrument
#
# ⚠ SIX repos have 0 tracked scripts/*.py (riir-auth, riir-game-sdk, riir-kat,
# riir-neuron-db, riir-viewbridge, mmorpg-remaster), so both quantities are
# 0 and neither detects anything there — Issue 783's population shape. What
# rescues those rows is that the gate's DOC_ROOTS handling is a REFUSAL, not a
# floor: a repo whose AGENTS.md the walk cannot see is an instrument failure.
#
# min_roots cannot be shared either: 1 in riir-shader, 13 in katgpt-rs.
#
# katgpt-rs's two floors are NOT free — they must equal
# instrument_reachability_gate.MIN_SCRIPTS / MIN_ROOTS, and the sweep's selftest
# ASSERTS that rather than trusting it. Same quantity, two files.
#
# Floors are ~60% of measured (the house slack convention); the ratchet is AT
# the measurement.
#
#                       min_scripts  min_roots  max_unreachable
katgpt-rs                       40          8                7
katgpt-web                       0          2                0  # observed 2026-09-18: no scripts/*.py
riir-ai                          4          2                7
riir-auth                        0          1                0
riir-chain                       1          3                3
riir-clippy                      3          2               11  # 4 -> 11 2026-09-18 (Issue 837): 7 plan/bench-scoped one-offs landed undocumented — plan146..151_frontier_probe, plan150_splice_freeze_tables, bench010_llama_arm, bench036_interleaved_ab, bench_bonsai_vs_clippy_tokps, split_rust_perf_corpus. The documented OVER-CAPTURE class (a plan artifact is not an instrument), so ratcheted at measured rather than documented into that repo's AGENTS.md — which is riir-clippy's own call. Surfaced only because the riir-llm UNPINNED red used to stop the sweep first.
riir-dao                         0          2                0  # observed 2026-09-18: no scripts/*.py
riir-dapps                       0          1                1
riir-deployer                    0          3                0  # observed 2026-09-18: no scripts/*.py
riir-esp32                       0          1                0  # observed 2026-09-18: no scripts/*.py
riir-game-sdk                    0          2                0
riir-kat                         0          1                0
riir-llm                         0          1                0  # observed 2026-09-18: joined the workspace 2026-09-18; no scripts, 1 root
riir-mmorpg-examples             1          1                2
riir-neuron-db                   0          2                0
riir-shader                      1          1                2
riir-train                      36          1               65  # 2026-09-17: 61 -> 65, ADJUDICATED not absorbed. The four are the Plan 403 cluster (build_capture_manifest, fetch_lph_corpus, modelless_floors, probe_verdict). Each name carries its plan number and each is a one-task artifact, which is the state this header calls CORRECT for this repo; modelless_floors is unreachable only because its two importers are, so the closure is working. Documenting them in riir-train's AGENTS.md would contradict that repo's own convention (it names none of its plan scripts). They were NOT left alone on the other axis: all five landed undefended streams too, and that was repaired at riir-train 21f19504 (Issue 804).
riir-viewbridge                  0          1                0
mmorpg-editor                 6          2                4  # Issue 842: first REAL measurement 2026-09-19 (6 scripts / 2 roots) — the alias-mapped open used to measure nothing. unreachable 1->4: bake_gltf_material_extras, migrate_materials, regate.py, strip_inter_pua are documented in the repo's own scripts/README.md, which is not a REACHABILITY ROOT (AGENTS.md / docs_gate.sh / workflows are) — making them findable in the repo's AGENTS.md is the editor owners' repair, not this pin's.
mmorpg-remaster             0          1                0
mmorpg-remake                      7          3                7  # Issue 842: first REAL measurement 2026-09-19 — 7 scripts / 3 roots / 7 unreachable. The set is the riir-train plan-scoped class: poc-check and GLB-inspection one-task tools (check_damage_popup_poc, inspect_glb_*, compute_weapon_grip, texture_audit...) whose whole life is one lane; documenting them in AGENTS.md contradicts nothing but is the repo owners' call.
#
# ⚠ FOUR rows are missing and this box cannot measure them: katgpt-web,
# riir-dao, riir-deployer, riir-esp32 are not checked out here (16 of 20).
# They report DEFERRED under DOCS_GATE_PARTIAL_CLONE=1 and will report
# UNPINNED on the canonical full workstation — pin them from one full-checkout
# run, alongside the same four rows owed by platform_dead_code_drift_floors,
# subprocess_encoding_drift_floors, orphaned_attr_drift_floors,
# wasm32_surface_drift_floors and len_derived_drift_floors.
riir-reflex                       0          1                0  # observed 2026-09-21: joined the workspace 2026-09-21 (Plan 603 T1.1 registration, owner-delegated repo birth)
riir-reflexer 0 1 0  # observed 2026-09-25: born public today (Research 457 P1 registration); md-only, no code yet; no scripts/*.py, 1 root (AGENTS.md), 0 unreachable
reflex-site 6 1 0  # observed 2026-09-26: joined the contract set today (BOUNDARY.md at reflex-site 89fa39c; findings repaired at 5eb0ae9/466e331/afdacd6 before pinning); 6 scripts/*.py, 1 root (AGENTS.md at 466e331), 0 unreachable
riir-infer                        0          1                0 # observed 2026-09-22: joined the workspace 2026-09-22 (riir-ai Issue 998 / reflex Issue 008 carve — Proposal 041 T-B fired)
