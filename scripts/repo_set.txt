# Derived, never typed. Regenerate with:
#   cd /Users/katopz/git && for d in */; do \
#     [ -f "$d/BOUNDARY.md" ] && [ -d "$d/.git" ] && echo "${d%/}"; done
# `-d .git`, not `-e`: a git worktree has a .git FILE.
# scripts/skill_repo_set_gate.py FAILS on the workstation if this drifts.
katgpt-rs
katgpt-web
riir-ai
riir-auth
riir-chain
riir-clippy
riir-dao
riir-dapps
riir-deployer
riir-game-sdk
riir-mmorpg-examples
riir-neuron-db
riir-train
riir-viewbridge
seal-game-editor
seal-remake
