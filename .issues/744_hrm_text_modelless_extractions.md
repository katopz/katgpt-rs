# Issue 744 — HRM-Text second-pass modelless extraction queue (8 candidates, 3 ranked)

**Status:** OPEN — queue, not commitment. Each item needs its own consumer + GOAT gate before code; "one consumer minimum" applies to #6/#8 (the refactor-shape items). Source: katgpt-rs `.research/547` §Path 0 merge table (No-GD advocate output, HRM-Text @ `aaa948ea674fd84b7bc455c9cfb455ecfefdf914`, Apache-2.0). Training-track sibling: `riir-train/.plans/394` — no cascade between tracks.
**Date:** 2026-09-11

## Ranked (GOAT-worthy)

- [ ] **#1 — `packing_sequence_sum`: one-cumsum segmented reduction** (HRM-Text `models/common.py` L16-18). Padded-cumsum + gather computes per-segment sums over a packed buffer in one O(n) branch-free pass — vs a per-segment loop. Owners/consumers: `katgpt-core` primitive; `riir-clippy/src/draft/` AstChunker-stream aggregates (chunk boundaries ARE cu_seqlens), riir-ai zone-delta byte sums pre-`flush_zones`, riir-neuron-db consolidation stats. Gate: G1 exactness vs naive loop on adversarial layouts (empty segment is the poison case — pairs with the empty-span prep law); G2 throughput floor vs per-segment at 10⁶ values / 10³ segments (expect 2–5×, must prove); G4 zero alloc.
- [ ] **#2 — atan2-homogeneity SNR gate + rank-invariance law** (property of the atan2 Adam update, second-pass extraction). `atan2(c·m, c·s) = atan2(m, s)` ∀c>0 → `sigmoid(α·atan2(dot, √energy))` is a normalize-free scale-invariant projection — no division, bounded, allocation-free, house bridge pattern. Rank-invariance corollary: ordering survives any per-tier positive rescaling — the shape wanted where evidence tiers are not a priori commensurable (riir-clippy `traj_store`/`elo` selection). Gate: G1 rank-invariance property test on randomized sets; G2 hot-path cost vs the normalize-then-gate incumbent on a real selection path; G3 incumbent behavior unchanged with feature off; G4 zero alloc. **UQ caveat:** ships as an ORDERING statistic only — if any consumer reads it as calibrated confidence, the conformal-naive floor rule binds and the gate must say so.
- [ ] **#3 — Seekable O(1) deterministic permutation** (upgrade over HRM-Text's materialized per-epoch Philox index files). `perm(i)` in O(1) time/space via counter-based RNG or Feistel + cycle-walking over [0,N): bijection, seedable, cross-process replayable, zero alloc — at N=10⁸, materialize-and-shuffle costs ~800 MB and O(N) to touch one element. Consumers: bench seed schedules, mining-batch ordering, packed-corpus shuffles, scenelab scenario orders. Gate: G1 exhaustive no-dup/no-omit incl. non-power-of-two N (cycle-walking is where implementations die) + cross-process replay bit-identity; G2 O(1) seek + O(1) memory vs materialize at N=10⁷–10⁸; G3 pin the algorithm version (a permutation change is a wire break for replay-dependent consumers).

## Recorded (consumer minimum before code)

- [ ] **#4 — fmod truncated normal (closed-form constant)** (`common.py` L10-13): `normal_().fmod_(3.0).mul_(1.014762601732121·std)` — fold-tails-branch-free then restore unit variance. Constant VERIFIED closed-form: E[(Z mod 3)²] ≈ 0.971117 → 1/√0.971117 ≈ 1.014763 (matches 5dp). Caveats: folded-tail ≠ rejection-truncated (~0.27% density bump near zero, kurtosis shift — init-grade, NOT statistics-grade); the win is branch-free bulk in-place init, not rejection avoidance (±3σ only rejects 0.27%). Existing `riir-train-gpu::trunc_normal_init` (Box-Muller ±2σ) stays the reference; adopt only with a bulk-init consumer (spectral shard init, SimHash seeding, ternary codebook jitter).
- [ ] **#5 — Init regime menu as variance budgets for constructed operators** (`transformer.py` L32-62): lecun `1/√fan_in`; megatron `out_std = in_std/√(2·n_layers)` — the closed-form depth-compensation that keeps a deterministically-constructed LoRA overlay's `‖Δ·x‖` a bounded fraction of the base stream through L layers (the modelless mandate's constructed-adapter path currently has no init half); scaled-embedding unit-variance-at-init. Consumers: katgpt-core constructors + riir-neuron-db spectral init / overlay construction. Gate: empirical variance of a 32-deep composed operator within ±10% of unit at every depth + deterministic replay.
- [ ] **#6 — Two-timescale budget law: slow-first, reserve-one** (from the H/L bp allocation `H_bp = min(H_cycles, B−1); L_bp = B − H_bp ≥ 1`). Inference-scheduler form: deliberation may consume the budget but never all of it — perception fires ≥1 per window. Candidate consumers: riir-engine cognition runtimes (CLR/KARC deliberation vs per-tick perception), riir-simloop tick budgets. **Needs a named consumer + a measured quality axis (scenelab scenario success at equal tick budget, slow-first ≥ uniform split) before any code** — without the quality axis this is a convention, not a primitive.
- [ ] **#7 — Condition tokens as modelless mode routing** (`prepare_sft_data.py` L50-54: direct/cot/noisy/synth vocab prefixes; one substrate, several behavior modes, zero param mutation). Track-(b) shape: one belief-kernel substrate serving several modes via a vocab-selected prior (condition → different `evolve_belief` rate constants / bit-plane masks). Gate: G1 mode separation on a fixture corpus + no-condition path bit-identical to legacy default; G2 routing cost ≈ one prefix write; G3 the default mode IS the legacy path.
- [ ] **#8 — Sigmoid-gated attention output, deterministic-gate variant** (Research 048's D6, `layers.py` L124-155): gate from fixed direction vectors / deterministic reader-LoRA (never trained weights in the modelless variant), `sigmoid(gate)·attn_out` keeps output in the convex hull of the ungated stream (the `Hla/Bounded.lean` invariant class); fused (gate,Q,K,V) projection halves launches (M3-Metal measurable). **Needs the HLA consumer to want the gate** — record-only until then.

## Convention-grade (no issue tasks; adopt as property tests at consumer sites)

- Saturating counters at bounded-buffer sites (cache lengths, ring buffers, WAL rotation): `len.add(1).clamp_max(max−1)` — never panics, saturate idempotent.
- Empty-span boundary law: hot kernels may assume `len ≥ 1`; degenerate spans rejected once at prep/boundary, never in the hot loop (HRM-Text's crash provenance: packed batch of only-empty responses kills FA3 backward).

## Non-goals

- No training-lane work here (that is riir-train Plan 394). No param-EMA primitive (weight-space — training track). No thaw-preview build on `MerkleFrozenEnvelope` (the envelope + `can_freeze` convergence gate already cover it; the EMA-over-runtime-state consolidation idea is recorded in Research 547's merge table as a note, not a task).
