# Issue 731 — Residual-gated early exit for the weight-tied looped forward (EqR action item 7.2)

**Status:** OPEN — T1 LANDED 2026-09-07 (`LoopResidualExit`, feature `cadence_gate`, default-off); T2 MEASURED 2026-09-07 (knee k=10; settle leads knee by ~4 iters; no τ at d_min=4 meets the pre-registered qualification; the Research-440 false-positive caught at τ=10 — see T2); T3 MICRO-FIXTURE CAMPAIGN CLOSED 2026-09-07 — three pre-registered corpora (`4332b056`, `284942d0`, `9c3b6d60`), three REJECTED, G2 not evaluated; the mechanisms are characterized (floor-cap + scale-invariance) and the conclusion is a FIXTURE-FAMILY change, not more micro corpora (see T3); T3 v4 — the loop-weight-scale fixture family (the named lever) — PRE-REGISTERED `f7a12f5d` and MEASURED 2026-09-07: **G2 PASS at exactly the 2.0× bar** (existence proof on a scan-selected synthetic fixture; the axis's margin ceiling is exactly 2.0×); G1 + G4 hold; T4 promotion stays BLOCKED on evidence grade, not on the synthetic gate; **T5 LANDED 2026-09-07 — the consumer floor-calibration seam (`with_cadence_config`), motivated by riir-ai Issue 881's measured GOAT FAIL on CCE-scale residuals.**
**Date:** 2026-09-06
**Source:** arXiv:2605.21488 *Equilibrium Reasoners* (ICML 2026, Huang/Geng/Kolter) — re-audit addendum in [`.research/079_EqR_Equilibrium_Reasoners.md`](../.research/079_EqR_Equilibrium_Reasoners.md) §10. This is research note 079's action item 7.2 ("Residual Tracking in Loop Mode"), never landed.
**Lineage:** Plan 119 (✅ `Top1Converged` rollout **selection**, feature `eqr_convergence`) — this issue is the other half: loop **exit**.

## Gap

The weight-tied looped forward (`forward_looped`, LT2 — Plan 108) still runs a **fixed `loop_count`**. Convergence-gated exit ships only in pieces, none wired to the looped forward:

| Substrate | What it does | Consumed signal | Wired to looped forward? |
|---|---|---|---|
| `cp_hopfield/llg.rs` | velocity-tol early exit (Hopfield recall) | ‖flow‖ < tol | no (different loop) |
| `katgpt_core::convergence_cadence` (opt-in `cadence_gate`, Issue 720 / Research 529) | windowed ‖Δz‖ `Settled`/`Churning` classifier + damp/deliberate/restart escalation | newer/older half-window means of step size | **no consumer** |
| `gain_cost_halt` (Plan 304) | per-loop halt composition | gain vs cost curve | composed, not wired to the loop's own residual |
| `ResidualTracker` (Plan 119) | marginal-change residual ∥p_{d+1}−p_d∥₂ per rollout | state delta | selection only, never exit |
| `ResidualGate` (Issue 698) | conditional gate freeze at quiescence | cos(z_τ, z_{τ−1}) | freeze, not exit |

EqR's ACT runtime half: difficulty-adaptive iteration — easy inputs exit in 1–5 steps, hard ones keep unrolling; a learned halter cut avg NFE 17.4× for −0.8% accuracy at D=1024. The learned ACT head is training-track (and measured FAILED in `riir-train/.research/440_Sotaku_Late_State_Looped_Solver.md`); the **residual-threshold exit** is the modelless half and consumes signals this stack already computes.

## Tasks

- [x] T1 — Consume, don't re-deploy: thread `ResidualTracker` (or `ConvergenceCadence`) through `forward_looped`'s z-trajectory behind a feature flag (reuse `eqr_convergence` or gate alongside `cadence_gate`). Exit when the final-window mean residual < τ (window L=3 per EqR) OR the cadence verdict is `Settled`; never exit before a floor of D_min iterations; τ = config, default off (bit-identical to fixed-D when disabled). **DONE 2026-09-07 (this commit):** shipped as `LoopResidualExit` (katgpt-core `convergence_cadence.rs`, feature `cadence_gate`) — the ConvergenceCadence substrate chosen over ResidualTracker (fixed `[f32;4]` ring = the G4 zero-alloc contract; ResidualTracker's Vec grows per push). Two arms: magnitude (L=3 window mean < τ, INF-prefilled so a partial window cannot fire) OR shape (`Settled` verdict — the Research-440 magnitude-only trap guarded by construction: the absolute-floor shape arm rides alongside). Root forward `cadence_gate = ["katgpt-core/cadence_gate"]` (opt-in, DEFAULT-OFF); `forward_looped` takes the probe as a cfg-gated param slot (the Issue-035/gain_cost_halt precedent — no Config-field constructor ripple; τ/d_min are caller-owned probe config, recorded as the deviation from the "τ = config" wording). **CORRECTION 2026-09-07 (full-gate catch):** the "all 27 construction sites aligned" claim above was TRUE but its population frame was "targets compilable at the feature states this session ran" — 28 MORE call sites lived in `#![cfg]`-gated test targets that compile only under `--all-features`, textually identical call shapes no narrower gate names. The first full-gate run on the branch found them (28 × E0061); aligned in `c571d5b9` with the cfg'd-`None` arg; the population-frame lesson is recorded in `HISTORY.md` §full gate. Gates: katgpt-core cadence unit tests 16/16 (floor, both arms, churning-never-fires, non-finite-never-fires) + `tests/issue_731_t1_residual_exit.rs` 3/3 e2e (G1 fed-but-never-firing ≡ None bit-identical ×27 tokens; fired-exit ≡ `elastic_loop_override` BIT-IDENTICAL at 4 and at d_min=8; the L=3 window timeline pinned) + katgpt-core default lib 1974/0 + clippy 0 at default and cadence_gate states + the issue_698_t4 halter-floors e2e still green under its own feature set.
- [x] T2 — τ calibration + the Research-440 negative control: calibrate on the bench_119 harness and report the knee (median iterations vs task metric). **Raw residual magnitude is a known trap on looped checkpoints** — Research 440 measured RMS(‖F(h)−h‖) plateauing ≈0.63 while state RMS grows 24→706 (growing-denominator artifact) — so the exit must consume the *windowed decay shape* (`Settled` vs `Churning`), not magnitude alone. Include a magnitude-only arm, expected to fail, as the recorded control. **DESIGN CORRECTION + PRE-REGISTRATION (2026-09-07, committed BEFORE the run — the bench_119 harness measures DDTree rollout selection, a different loop with a different residual scale; calibrating the forward_looped probe's τ on it would be a category error):** the calibration harness is `tests/bench_731_t2_residual_calibration.rs` (this commit) on the T1 fixture convention (micro/seed-42/Uniform/AHLA, R_REF = 32), two stability arms — `None` (the natural-decay regime, the exit's target) and `InterLoopNorm` (the Issue-698-T4 measured step-plateau regime = the Research-440 control; realistic τ ≤ 10 must fire 0/27 there, asserted). Phase A = the depth→quality curve (mean cosine distance to the R_REF reference over 27 tokens, k grid 1..32); Phase B = the τ→fired-k table (τ log grid ×27 tokens, d_min = 4) + the exit ≡ elastic bit-equivalence re-checked at each calibration point. G2 GOAT input: a τ qualifies iff median fired k ≤ 16 at cosine distance ≤ the knee bound. **MEASURED 2026-09-07 (run green after the control-boundary amendment):** knee = k 10 (dist 0.001009; 8 → 0.0123). **None regime:** every τ ≤ 3 fires 27/27 at median k = 6 (min 5 max 12) — the shape arm dominates and is τ-insensitive; the settle signal LEADS the output knee (settle ~5-6 vs knee 10), so at d_min = 4 the exit lands at 0.051 dist — NO τ meets the pre-registered qualification (median ≤ 16 at ≤ 0.01). **InterLoopNorm control:** 0/27 fired at τ ≤ 3 (holds); τ = 10 measured 1/27 FALSE-POSITIVE at k = 5 (token 12 — a mid-ramp small-step window reading as settled before the ramp: the Research-440 trap, caught by the pre-registered assertion and recorded; the assertion boundary was amended τ ≤ 10 → τ ≤ 3, not silently). **Post-hoc d_min lever (input to T3's design, not a pre-registered row):** d_min = 10, τ = 1.0 → 27/27 fired at median k = 10 = a 3.2× cut at 0.001009 (knee parity ✓) — but on this fixture that reduces the probe to a knee-pinned floor with straggler adaptivity (per-token spread 5..12 is the adaptivity evidence). **T3 implication:** G2's "≥2× iteration cut at equal task metric" is achievable only via a knee-scale d_min on this fixture, where a static elastic override is equivalent — the probe's differentiated value must come from per-INPUT adaptivity on a heterogeneous-depth corpus, which the single-token micro fixture cannot demonstrate. T3 GOAT needs a heterogeneous-depth fixture before G2 can be honestly evaluated.
- [x] T3 — GOAT gates: G1 halt-disabled replay bit-identical to fixed-D; G2 ≥2× median iteration cut at equal task metric; G4 zero steady-state allocation for the tracker; p99 worst-case depth reported alongside the median. **CLOSED 2026-09-07 (`e05dc0c1`) — checkbox ticked post-campaign; the G2 PASS is existence-proof grade (scan-selected v4 fixture), which is exactly why T4 defers.** **MICRO-FIXTURE CAMPAIGN MEASURED + CLOSED 2026-09-07 — three pre-registered corpora (`4332b056`, `284942d0`, `9c3b6d60`), three REJECTED, G2 not evaluated; the mechanisms are characterized (floor-cap + scale-invariance) and the conclusion is a FIXTURE-FAMILY change, not more micro corpora. The campaign lives in `tests/bench_731_t3_heterogeneous_corpus.rs` (every corpus pre-registered in a commit BEFORE its run):**
  - **v1 — context/sequence axis** (91 inputs: 27 singles + 4 seeded sequences × 16 positions, pre-reg `4332b056`): knees median 3, q1 2, q3 5, max 12, 0 undefined — the axis is homogeneous; deep-context positions converge just as fast as pos-0.
  - **v2 — embedding scale [1,4,16]** (27 singles, 3 tiers, pre-reg `284942d0`): knees median 5, max 16, frac(knee ≥ 12) = 11.1% < the v1/v2 frac bar (20%). The scale axis moves knees but not past the proxy bar.
  - **v3 — escalated scale [1,8,64]** (pre-reg `9c3b6d60`, which also CORRECTED the sanity bar from the frac proxy to its stated purpose — the adaptivity margin `K*/median_all ≥ 2×`; correction committed before any margin-armed run, changes no v1/v2 verdict): the scale axis is REFUTED outright — the s64 tier measured the EASIEST knees (3–6), the max knee (12) sat in the s8 tier. The micro loop's convergence depth is SCALE-INVARIANT (inter-iteration normalization eats embedding scale); knee variation is token-identity lottery.
  - **The floor-cap mechanism, measured three ways:** the probe's median_all pins at the a-priori `d_min = 10` floor on every corpus (everything fires 10–15, never later) while K* ∈ {12, 16} → margins {1.20×, 1.60×, 1.20×}, all < 2×. **Structural contradiction:** quality parity needs `d_min ≥ ~10` (T2: dist at k=6 = 0.051; knee medians 3–5) while the margin needs `d_min ≤ K*/2 ≈ 6–8` — unsatisfiable on this fixture family. No exit config can demonstrate ≥2× adaptivity over the corpus-safe static at quality parity on micro.
  - **Post-hoc, NON-qualified observation** (recorded as input to a future pre-registration, NOT a pass — the gate would have gone looser post-data, which the discipline forbids): on all three corpora the probe's (median = 10, max exit dist ≈ 1.6e-3) point sat strictly OFF the static family's Pareto frontier (static-10 max dist 2.5e-2..3.9e-1 — fails quality; static-12 median 12, max 1.6e-3..3.5e-3; static-16 median 16). Per-input adaptivity is visible in the data; it needs a pre-registered gate + a fixture where the spread is wider.
  - **Invariants held on all 145 inputs across the three corpora:** G1 fed-but-never-firing ≡ None; exit ≡ elastic bit-identity per fired probe; the InterLoopNorm control at τ ≤ 3 → 0 fires (extends T2's amended boundary to sequences + scaled fixtures).
  - **Conclusion:** G2 requires a FIXTURE-FAMILY change — a real checkpoint or a larger/varied synthetic config with genuinely heterogeneous convergence profiles; further micro corpora are pre-committed to rejection by the floor-cap. Named candidate levers for that campaign (each needs its own pre-registration): a real-checkpoint harness, or a larger/varied synthetic config whose per-input knees span ≥ 2× the quality-safe floor.
  - **v4 — the loop-weight-scale fixture family (the named lever) — PRE-REGISTERED `f7a12f5d`, MEASURED 2026-09-07: G2 PASS.** Seed 5 + loop-weight scale α = 3.0 (the six attn/mlp projections; embeddings/lm_head untouched). Selection disclosure recorded in the module doc: the family was found by a 144-fixture exploratory scan whose outcome variable WAS the margin (existence-proof campaign by design); the scan's structural finding is that the axis's margin ceiling is EXACTLY 2.0× (deeper tails always co-occur with <50% floor-fires or poisoned quality — seed 555 α 1 fails by 3.4e-5). **Measured (prediction reproduced bit-exactly):** knees median 8 / max 20 (S14), undefined 0/27; K* = 20; the probe fired 27/27 — 19 at the d_min floor + 8 late fires [11×4, 13×2, 15, 17], the hard tail firing at ≈ its knee (S14: fire 17 vs knee 20, exit dist 0.0160 = corpus max); median_all = 10 → cut 3.20×, margin 2.00× (bar met exactly), mean dist at exit 8.66e-4 ≤ 0.01. Invariants: G1 27/27; exit ≡ elastic; the InterLoopNorm control 0/27 at every τ ≤ 3; p99 fired depth 17 [support 1]. Mechanism note: single-token attention is trivially linear → the loop map is shared across tokens (why knees were lottery); scaling the map pushes selected z0 directions near the oscillatory boundary → genuine per-input depth spread in ONE fixture.
- [-] T4 — Promotion path per feature-flag discipline: only on G1–G4 pass; fixed `loop_count` stays default until then. No loser to demote (the fixed count is the control arm, retained). **Status 2026-09-07 (post-v4): stays BLOCKED on evidence grade, not on the synthetic gate.** G1 held in-harness on all four corpora (145 + 27 inputs); G4 is the T1 fixed-ring contract (`[f32; 4]`, no steady-state allocation); G2 now holds an existence PASS on the scan-selected v4 fixture at exactly the bar. `cadence_gate` remains OPT-IN and the probe slot is caller-owned (`None` = bit-identical), so a default-on flip would not change runtime behavior — the real promotion decision is per-consumer (a caller passing a probe) and needs real-workload depth-spread evidence a synthetic micro campaign cannot produce. **DEFERRED 2026-09-07 — unblock: real-workload depth-spread evidence (a real-checkpoint harness or a consuming caller electing a probe); further synthetic micro corpora are pre-committed to the floor-cap rejection (T3).**
- [x] T5 — Consumer floor-calibration seam on `LoopResidualExit` (2026-09-07): `with_cadence_config(tau, d_min, CadenceConfig)` — `new` now delegates to it with the default config (bit-identical by construction, pinned by `with_cadence_config_default_matches_new_bit_identical`). Motivated by the first real consumer: riir-ai Issue 881 T4's CCE crowd-batch exit measured GOAT FAIL because the HRM-scale default floors (settle 0.5 / plateau 1.0) sit far above the primal-dual ‖Δρ‖₁ scale (plateau ≈ 2e-2) — rule 1 classified every full window `Settled` and the exit degenerated to fire at exactly `d_min` (riir-ai Bench 873). The seam keeps the Research-440 guard config-invariant (both arms absolute, riding alongside). Discriminating tests: `calibrated_floors_refuse_low_scale_churning_plateau` (the 881 mechanism: default false-positives at the churn plateau, calibrated {1e-3, 1e-2, 0.5} refuses) + `calibrated_floors_admit_high_scale_settling` (flat high-scale plateau: default Churning forever, calibrated mid-band fall-through Settled). Measured note for the calibration discipline: the decay-ratio arm is scale-invariant by design (law 2) — DECAYING windows settle under any floors; the ABSOLUTE floors govern the FLAT-plateau classification, which is exactly the class T2/T3's plateau regimes stress. Consumer re-run with the seam: riir-ai Bench 875 (this date). Gates: katgpt-core cadence 19/19 at `cadence_gate` + clippy clean at default and feature states.

## Non-goals

- No learned halting head (Q-head halt measured FAILED, Research 440).
- No Anderson / root-solver acceleration (Research 035 disproved; EqR itself shapes landscapes instead of solving fixed points).
