# Issue 883 — fitted token-value tables (the V-side anchor-scoring twin: K=V+ retrofit, mean-removed V quant, V-cache halving)

**Status:** OPEN — P0 is one offline calibration pass (no GD, no training). Research: [587](../.research/587_Memory_Attention_Fitted_Token_Value_Tables.md) · twin: [Issue 882](882_differential_anchor_scoring.md) (Q-side anchors — SHARED table-builder substrate) · training arm: riir-train Plan 418.

## Problem

Memory Attention (arXiv:2609.28399) shows `V = K + Norm(E_l[s])` is a *sufficient* value pathway when trained from scratch (better PPL, NIAH 97.4 vs 82.6, 1.42× token efficiency). Research 165 showed K/V redundancy (cosine 0.73) — `V := K` costs only 2.5–3.1% PPL and halves the V cache; Gemma-4 ships the trained variant (`attention_k_eq_v`, our riir-infer loader already falls back V=K). **What ships nowhere: fitting the dropped residual per token** — the per-(layer, token) mean of `V − K` is a closed-form estimator on a frozen checkpoint, and it converts "K=V sharing with a known quality tax" into "K=V+ with the tax partially refunded", plus a variance-bounded cache-quant decomposition (variance cannot increase; the quantizer-level gain is measured, not proven — P1), plus V-cache halving by reconstruction. Same estimator family as Issue 882's Q-side anchors (`q̂ = q − λ·ā`): here `v̂ = k + λ·ē_l[s]`.

## The primitive

One calibration pass over ~10⁸–10⁹ tokens (WikiText/FineWeb slice) through a frozen checkpoint with V/K taps at every layer yields, streaming and closed-form:
- `E_l[s] = mean(V_t − K_t | s_t=s)` (pre-RoPE K tap) — the retrofit table, shrunk `E_λ[s] = n_s/(n_s+λ)·mean_s` (James-Stein tail control; λ by direct evaluation on held-out fixtures, the 882 discipline, never GD);
- `E^V_l[s] = mean(V_t | s_t=s)` — the value-mean table for the quant decomposition;
- the **R² dashboard**: per-layer per-head token-explained fractions ρ_l(V), ρ_l(V−K) from accumulated sufficient statistics (law of total variance) — the offline go/no-go for every task below;
- Zipf coverage curves for top-K table residency.

Every product's expected gain is a monotone function of ρ_l, computable before any serving change. Tables are `EngramTableId`-committable, `ZipfianCacheHierarchy`-resident (top-K dial), `EngramHotSwap`-swappable — the Plan 408 deterministically-constructed-values legality class.

## Tasks

- [ ] **P0 — calibration taps + R² dashboard** (`katgpt-transformer` instrumentation + a `katgpt-core` accumulation module; shares the table-builder with 882 P0): streaming sum/count per (layer, token, head) for V, K, V−K; per-layer ρ_l, per-token n_s histogram, coverage(K) curves. Output: the go/no-go report per model. **Fixture order — mechanism-bearing first: gemma-2-2b-it** (`riir-train/data/gemma-2-2b-it-f16.gguf`, on disk; GQA, standard K/V cache — the one local fixture where P1/P2/P3 ALL apply; its QK-norm tap-point wrinkle is trap 1, handled at tap definition — not a reason to defer). **Kimi-K3 0.4B second, DASHBOARD-ONLY** (6/8 layers are KDA linear attention — no KV cache at all; 2/8 are MLA — the cache IS the compressed latent, K and V both up-projections of it — so P1/P3 land on zero layers and P2's W_V is not separable there). A ρ_l ≈ 0 reading on Kimi is a FIXTURE-class null (linear-attention family), never recorded as a model null (trap 4). **No quality claim at P0 — measurement only.** G4: streaming accumulators alloc-free per layer.
- [ ] **P1 — token-mean-removed V-cache quantization** (the variance-bounded product; lands on the KVarN-159 / massive-activations-487 lane): write `V̂ = V − E^V_l[s]`, quantize V̂, add the table row back at read. Law-of-total-variance bound: residual VARIANCE cannot increase (equality iff token identity carries nothing) — that part is proven. **The quantizer-level claim is MEASURED, not proven: per-group absmax integer quant scales by RANGE, which mean-removal does not bound (an off-mean occurrence grows |V̂|); the variance→quant-error step holds for MSE-optimal / uniform-scalar quantizers.** G1: PPL at matched bits with/without removal **+ per-family conditional retention walk (lossy surface — the riir-ai Bench 948 pattern, aggregate PPL alone disqualified)**; the dashboard's predicted variance reduction vs measured quant-error drop must agree (prediction-vs-measurement is the gate — the arbiter for the absmax caveat above). G2: fused dequant+add ≤ +1% kernel time (rides the LUT/dequant epilogue). G3: E=0 bit-identical. Consumer: riir-ai/riir-infer KV-quant paths + `kv_sink_window` fallback for table-missed tail tokens.
- [ ] **P2 — fitted K=V+ retrofit PoC** (W_V-free serving on a standard checkpoint): serve `V = K + λ·E_l[s]` with W_V deleted. G1: held-out PPL + **the NIAH harness `benches/bench_685_flashmemory_full_model_niah_qa.rs` (Bench 814)** at λ ∈ {0, 0.5, 1} × per-layer schedule vs (a) full V and (b) V:=K — the claim under test is only `quality(K=V+) > quality(K=V)`, i.e. the table refunds part of the 2.5–3.1% tax; **§3.6 discipline: no parity claim vs full V is made or expected — record the measured ladder honestly.** G2: decode µs/token with the V GEMV deleted — **protocol: paired A/B via `tests/common/ab_timing.rs` (never two sequential arms); `black_box` on result AND arguments; box state recorded (free RAM, commit-vs-limit, concurrent jobs)**. G3: λ=0 (table=0) bit-identical to the V:=K path, pinned. G4: lookup+add alloc-free (engram_micro precedent).
- [ ] **P3 — V-cache halving by reconstruction** (the MA-Recall analog — the paper analyzes it WITHOUT measuring it; this task is the measurement, in our retrofit context): drop the persistent V cache; reconstruct `V_t = G(−θ·p)·K̂_t + E_l[s_t]` from the cached (post-RoPE) K via `RopeAction::apply_inverse_at` (in-tree, roundtrip-tested) — one one-directional rotation per read, never a round-trip. Target: the **tg128 league cell** (KV bytes/token −50% at equal n_kv/n_v). G1: PPL within the P2-measured tolerance + per-family retention walk. G2: tg128 tok/s vs full-cache control, same protocol as P2. G3: kill switch to full cache, bit-identical when selected. Correctness traps: tap-point = where the CACHE stores K (post-QK-norm for gemma-class, pre-RoPE position recovery via position metadata); sliding-window layers scale with the window (still exactly the V fraction).
- [ ] **P4 — laws + docs rider**: verify the FLOP law `ΔF_V ≈ LSd_v(2d−1)` against the perf ledger (two-line bench); document the storage dial `P(K) = b_w·L·K·d_v` + coverage curves; the 50%-cache law with GQA generalization `n_v/(n_kv+n_v)`.
- [ ] **Promotion**: any task promoting to default requires its GOAT pass + the loser demoted (standing rule). The measured-null outcome (ρ_l ≈ 0 on a mechanism-bearing fixture ⇒ K=V sharing already optimal for that checkpoint; fixture-class nulls excluded per trap 4) is a LEGITIMATE recorded negative — record it, never force λ.

## Known traps (panel-derived, load-bearing)

1. **Tap-point consistency** — the fit must tap K exactly where the cache/projection stores it (pre-RoPE; post-QK-norm for gemma-class). A mismatched tap is silent corruption.
2. **Never round-trip rotations** — single one-directional rotate per read; f32 error compounds otherwise.
3. **Sinks are legitimate outliers** (882 trap 3, same here) — the per-token table ABSORBS sink means rather than exempting them; verify sink behavior doesn't regress at P1 via the shipped `kv_sink_window` machinery.
4. **ρ_l ≈ 0 is a valid answer — on a mechanism-bearing fixture only** — if token identity explains little of V−K on a GQA/MHA checkpoint with a real K/V split (e.g. gemma-2-2b), K=V was already right for that checkpoint; a null on a KDA/MLA fixture is FIXTURE-class (the architecture lacks the mechanism), never model-class (P0). The dashboard exists to say so before anything is built.
5. **λ by direct evaluation, never GD** — grid on held-out fixtures, per-layer schedule allowed; λ=0 must be bit-identical.
6. **Quality claims need the retention walk** — lossy surfaces gate per-family conditional retention, never aggregate-only (the riir-ai Issue-750-T3 / Orthrus law).
7. **Box state on every perf figure** — the G2 law: free RAM, commit-vs-limit, concurrent jobs, power state on laptops.
