---
name: boundary-guard
description: Audit + enforce game-stack boundary rules across the multi-repo workspace. Use when adding a System impl, game logic, vocabulary type, FFI surface, or view-layer code; when reviewing PRs touching game systems or view/FFI boundaries; when a violation is suspected; or quarterly as a boundary-hygiene gate. Reads each repo's BOUNDARY.md as the contract — owns, not-owns, dep allowlist, drift ledger — enforced via ci_boundary_contract.sh (workspace dep graph + contract honesty, all 15 repos) + ci_boundary_guard.sh (per-repo code logic) + grep checks. Sibling to feature-gate-audit + goat-audit + doc-sync.
---

# Boundary Guard

Generic game logic → substrate (`riir-games`). View renders state, doesn't compute it. FFI moves raw bytes only.

## Spec source — each repo's `BOUNDARY.md`

Every repo ships a root [`BOUNDARY.md`](../../riir-ai/BOUNDARY.md) — the per-repo
contract this skill audits against: **Owns** / **Does not own** / **May depend
on** (crate-granular allowlist with Location) / **Inherited** (links) / **Drift
ledger**. The 8-surface table below is the workspace *methodology*; the per-repo
rules + exceptions come from that repo's BOUNDARY.md, not from this file's prose.
Findings are therefore two classes: **code-vs-contract violations** and
**contract rot** (drift row without an open issue, allowlist row without a gate,
by-design row whose decision record is gone).

**Two scripts, one job each** (added 2026-08-21, Issue 737 T-CI/T-GATE):

| script | scope | question it answers |
|---|---|---|
| `riir-ai/scripts/ci_boundary_contract.sh` | workspace (every repo with a root `BOUNDARY.md` — **18** at the 2026-09-01 run; the script enumerates, so prefer its banner over this cell) | Is the dep graph what the contracts say — and are the contracts still honest? Parses every `May depend on` table + drift ledger, checks the riir-ai CANONICAL matrix against the measured graph, pins the 4 split-prep invariants. `--list-deps` prints the measured edge set; `--repo X` narrows. |
| `riir-mmorpg-examples/scripts/ci_boundary_guard.sh` | that repo's `src/` | Is the CODE in the right repo? Checks A–E: System impls with game logic, duplicated geometry, hardcoded behavior constants, generic logic in free functions, facade leaks. |

The contract script replaces prose-only allowlists; it is the successor of
Issue 724 Phase 2's "document the contract and review by hand". `EXEMPT_LEAKS`
in the per-repo guard deliberately STAYS file-granular — the ledger is
surface-granular, and collapsing the two would lose the file:line precision
Check E needs.

**Drift-ledger semantics** (scripts consume this): Disposition ∈ `fixable` |
`owner-call` | `by-design`. `fixable`/`owner-call` rows REQUIRE an open issue;
`by-design` rows cite a decision record instead. Issue closes → row removed in
the same commit. Exit semantics: ledger unparseable → hard error; finding mapped
to a row → exit 0 with a LOUD known-drift count; unmapped finding → exit 1;
row-without-open-issue → exit 1 (rot). Never silently fail open or closed.

## Eight surfaces

| # | Surface | Rule | Grep check |
|---|---------|------|------------|
| 1 | Consumer `src/` (riir-mmorpg-examples, seal) | Thin glue only — no `impl System` with loops/math; no hardcoded constants; no duplicated helpers | `grep -rn 'fn distance_2d\|const.*FEAR' src/` |
| 2 | SDK root crate (`riir-game-sdk/src/`) | Facade only — no engine/chain/db deps in the DEFAULT build. Sanctioned opt-in exceptions (Issue 053 Part 2 + `auth_impl`/`gm` pattern; all `optional = true`, heavy ones target-gated native): `auth_impl`/`identity_impl` (riir-auth, riir-chain/ssh_key), `gm` (katgpt-core, hoisted `InferenceBackend`), `static_data_impl`/`warm_tier_impl` (riir-neuron-db, neuron-db-sdk). Scan BOTH `[dependencies]` AND `[target.'cfg(...)'.dependencies]` — the Issue 053 deps live in the target-gated section | main + target sections, filter out `optional = true` lines |
| 3 | SDK root vs workspace members | Root crate clean; members (`crates/riir-viz`, etc.) MAY depend on engine | `sed -n '/\[dependencies\]/,/^\[/p' riir-game-sdk/Cargo.toml \| grep katgpt` |
| 4 | Leaf-clean vocabulary (`riir-games-shared`) | No engine deps unless feature-gated — **at the dep level too**: dep line `optional = true` AND its feature carries `dep:<name>`. Half-gated (module cfg-gated, dep non-optional) = violation — every no-features build pays the engine tree (Issue 682: katgpt-core non-optional pulled rustfft/postcard/half into a default-`[]` crate) | `grep -E 'katgpt-core\|riir-engine' riir-ai/crates/riir-games-shared/Cargo.toml \| grep -v 'optional = true'` |
| 5 | View consumers — **live: `seal-remake` (`seal-view` Bevy/wasm + `seal-node`)**; the C# that remains in the workspace is `riir-viewbridge/csharp/` (3 files, surface 6's own side) | Rendering + input only — NO game logic (AI, combat, physics, sync). Documented deliberate debt in an OPEN issue + cross-language contract doc = record as such, don't re-file | `grep -rnE 'sigmoid\|dot_product\|impl .*System for' seal-remake/crates/*/src/ \| grep -viE 'showcase\|benchmark\|camera'` (measured 2026-09-04: **empty**) |
| 6 | FFI bridge (`riir-viewbridge`) | Raw physical only (`pos[3]`, `rot[4]`) — NO latent state crosses FFI | `grep -rn 'emotion\|fear\|mood\|curiosity' riir-viewbridge/crates/*/src/` |
| 7 | Dev tools (`riir-clippy`) | Zero game-domain coupling in the DEFAULT build (only `katgpt-core`, the public primitives crate). Sanctioned opt-in arms where reimplementation would duplicate whole substrates: `ternary_inference` (riir-engine + riir-gpu), `latent_retrieval` (riir-rag) | `grep -E 'riir-games\|riir-chain' riir-clippy/Cargo.toml` (must be empty; engine/rag allowed only `optional = true` behind their features) |
| 8 | dApp layer (`riir-dapps`) | One-way **game → dapps → chain** — never a game dep here (`scripts/direction_gate.sh`); a `Settlement` stays chain vocabulary (never quest/kill/recipe — gate check 3 of direction_gate.sh); anything a game wants on-chain passes the **three-test rule** (product / value / rate) on the **agreement axis** — the chain hosts what mutually distrusting parties must agree on; neuron-db hosts authenticated durability. A paid quest still needs NO chain program (neuron-db template + the one generic `MultiClaimEscrow`) | `grep -E 'riir-games\|riir-game-sdk\|riir-engine' riir-dapps/Cargo.toml` + game-vocab scan in `scripts/direction_gate.sh` |

### The dApp three-test rule (surface 8 detail — riir-chain Issues 096/097 + riir-dapps Proposal 001)

1. **Product** — would a commerce customer of this chain want it in their dependency? (An NFT is a token → yes. A quest/recipe/kill-credit predicate → no.)
2. **Value** — BigInt fungible currency, a token, or an authority binding? Not FAME/XP/items/reputation/karma/quest progress.
3. **Rate** — quorum-coordination ops are Glacial (≤0.1 Hz); settlement transactions are **capacity-share bound** (783 req/s measured floor) — different limits, both binding.

The defining axis is **agreement** (Research 003 §"The Second Axis" "Must agree on" column) — value and rate are the disqualifying tests, not the definition. Full argument + failure-mode matrix: `riir-dapps/.proposals/001_agreement_boundary_and_tiered_durability.md`.

All greps should return **empty** (clean), modulo the sanctioned opt-in exceptions noted per-surface.

<!-- retired surfaces, kept as lineage: `riir-unity` (C# Unity host) and
`seal-remake-unity` were moved to `git/obsolete/` by owner act on 2026-09-04,
`riir-armageddon` on 2026-09-02. Surface 5's old command globbed
`riir-unity/**/*.cs` and could no longer run — worse than stale, because a
non-matching zsh glob aborts the whole command line rather than returning
nothing, so the check would have looked skipped rather than broken. The
`WireProtocol.cs` precedent it cited (riir-unity Issue 002 Phase A, a
cross-language contract documented on both sides instead of re-filed) is the
part worth keeping and applies to `riir-viewbridge/csharp/` under surface 6. -->

**Methodology lesson (2026-08-15 run):** exclusion filters can hide exactly what you're looking for — the "who enables feature X" grep returned zero because the forwarder lines contain `katgpt-core` and were killed by `grep -v katgpt-core`. Vocabulary-translation care applies to filters, not just search terms.

## Failure pattern

"Helper" in consumer → wrapped in `System impl` → grows loops + math → stuck in consumer. Same applies to C# view code reimplementing substrate logic.

## Extraction checklist

Before adding to consumer `src/` or view C#/Bevy:

1. Is this generic game behavior? → substrate (`riir-games`)
2. Does substrate already have it? → grep `riir-games/src/{swarm,motivation,combat}/`
3. Can it be parameterized? → trait (`ThreatSource`) or config struct
4. Is the consumer/view just data + wiring? → if loops/math/constants present, STOP

If unsure → file an issue, don't add the code.

## Filing violations

1. `.issues/NNN_boundary_*.md` in the repo
2. Reference which surface (1–7)
3. Include file:line + grep output
4. Propose extraction target (substrate module + trait)
5. **Issue BEFORE fix** — every fixable finding gets its `.issues/NNN_boundary_*.md`
   filed BEFORE any fix commit, even trivially-fixable ones. The fix commit
   references the issue; closing the issue removes the drift row in the SAME
   commit. Only the guard/script tooling itself may be fixed in-run — boundary
   CODE never.

## Move by script, never regenerate

Any relocation of boundary content — extracting superseded sections from
AGENTS.md into BOUNDARY.md, removing drift rows, linking READMEs, or any future
crate/repo move — is done **by script** (`git mv` + anchored sed/python) with a
before/after **grep-parity check** (every rule sentence present exactly once
post-move). Re-typing or regenerating the content is forbidden. Grounding:
the AGENTS.md section silently dropped by a concurrent session's stale-buffer
commit (`88e5f98`), and the edit-fuzzy-match that ate a raw-string `#`
terminator — both would have been caught by parity checks.

## Running

```bash
# workspace contract (dep graph + contract honesty + split-prep gates)
cd riir-ai && ./scripts/ci_boundary_contract.sh          # exit 0 = clean
./scripts/ci_boundary_contract.sh --list-deps            # measured edge set
./scripts/ci_boundary_contract.sh --repo riir-chain      # one repo

# per-repo code logic (surface 1)
cd riir-mmorpg-examples && ./scripts/ci_boundary_guard.sh # exit 0 = clean
```

Exit codes (contract script): 0 clean or all-findings-mapped (LOUD known-drift
count), 1 unmapped finding or contract rot, **2 hard error** — a missing or
unparseable contract never fails open. For other repos' code-logic checks,
adapt the guard's SRC_DIR + patterns. Or as pre-commit: `exec ./scripts/ci_boundary_guard.sh`

**Run boundary checks VIA this skill** — not as ad-hoc greps. The skill reads
each repo's BOUNDARY.md as the contract, applies the methodology below, and
records the run in the log. The T-CI/T-GATE wiring landed 2026-08-21, so the
full via-skill run is now available (and the first one is logged below).

## Run log

| Date | Runner | Verdict |
|---|---|---|
| 2026-09-04 (membership change, contract NOT re-run) | katgpt-rs idle unit (docs-gate red on `repo_set.txt`; grep/parse only, no cargo — 8 sessions live on the box) | **MEMBERSHIP: 19 → 16 repos.** `riir-burner`, `riir-unity` and `seal-remake-unity` were moved to `git/obsolete/` at 00:01 by owner act, the same disposition as `riir-armageddon` on 2026-09-02 — directories intact there, nothing lost (riir-burner's last sweep pushed `ce54122`, 19 min before the move). **The 19th run's `19 repos / 225 edges` above is therefore NOT current and this row does not replace it:** three repos leaving must have moved the edge count and no `ci_boundary_contract.sh` was run here (the box is carrying 8 sessions; a full contract run is the next idle unit's work). What was fixed: `scripts/repo_set.txt` regenerated (16), AGENTS.md §"Repo count" membership + both counts, and the two SKILL.md **routing** surfaces that would have sent an agent to a repo that no longer exists — this file's surface 5 (its `riir-unity/**/*.cs` glob could no longer run, and a non-matching zsh glob aborts the command line rather than returning nothing, so the check would have read as skipped rather than broken; re-pointed at the live `seal-remake` Bevy viewer and measured empty) and doc-sync's per-repo table (which carried 18 rows over a 16-repo workspace — three retired ones present and `seal-remake` never added, wrong in BOTH directions). Caught by instrument, not by reading: the katgpt-rs repo-set gates went red within the hour, which is the second consecutive membership change they have caught. |
| 2026-09-03 (19th run — post-847/Plan-530-reconciliation delta check, zero-build; the box is loaded — M3 carries the sibling NoRA SFT probe (20.5 GB RSS) + a parallel clippy sweep, the 4090 the Issue-483 recovery arc (`bonsai_clippy_l4_eval` + `plan318_sft_train`, both started today) — parse/grep only, the Plan 337 discipline; sibling trees verified first: riir-ai synced to `f5e8c430e` (this session yielded a duplicate Plan-530 doc-sync edit mid-flight — the 4090 session landed `f5e8c430e` first; revert + ff, zero overlap), mmorpg clean at develop==origin) | idle-loop unit (riir-clippy skill item 6; full-workspace `ci_boundary_contract.sh` + S1 mmorpg guard) | **CLEAN — exit 0 both: contract 19 repos / 225 edges, SP1–SP5b + C6 + C9 green, 1 LOUD known-drift (the sanctioned `katgpt-device-verify` patch-coverage row, Issue 759 D1 — unchanged); S1 zero violations.** Advisory recorded, no issue (the C8 script comment says staleness disposition is an owner call): the 5 lockfiles pinning katgpt-rs `main` @`6f392727` (2026-08-24) are now **326 commits behind develop with 3 security fixes in the gap** — `99afbab9` (SIMD len-family OOB read + heap-corrupting write), `5b028c00` (the katgpt-dec `simd_dot_f32` twin), `af2e48e1` (wasm FFI catch_unwind) — exactly the classes the pinned consumers run (katgpt-core SIMD kernels; the wasmi validator surface). Owner-facing bump decision (chain node Docker + 2 CF container roots), not an idle-unit act. |
| 2026-09-02 (18th run — post-Plan-031-Phase-5-split contract re-measure; the seal Unity host is now the standalone `seal-remake-unity` repo, so the workspace contract needed a re-run to enroll it) | session unit (user-directed Plan 031 fallout; full-workspace `ci_boundary_contract.sh` + the katgpt-rs repo-set gate; parse-only, no cargo) | **CLEAN — exit 0, 19 repos / 225 edges, `seal-remake-unity` enrolled with ZERO edges (no Cargo manifests — the gate measures it as a pure view host; joins the riir-ai BOUNDARY.md zero-dep row `132bf5214`). The 227→225 edge delta is EXACTLY the armageddon de-enroll completing: its 2 allowlist edges (riir-games + katgpt-core) left the measured graph with the directory (owner act, now on disk), the new repo adds 0. The katgpt-rs repo-set gate caught the stale snapshot mid-session (missing seal-remake-unity, still listing the gone armageddon — the instrument working); snapshot updated katgpt-rs `d2cb9979`. Sibling-WIP caution honored: riir-ai committed ONLY the BOUNDARY.md hunk (gpu/issues files are the sibling's), katgpt-rs ONLY repo_set.txt. Known non-boundary red left for its owner: katgpt-rs docs gate `count_features` 568→569 (sibling katgpt-core WIP mid-flight). One stale-command incident recorded: the OLD `cargo unity --unity-root ../seal-remake/unity` doc resurrected the deleted folder (2.6 GB codegen churn, verified zero unique files, removed same day; docs corrected across viewbridge/unity/mmorpg). |
| 2026-09-01 (16th run — scoped delta spot-check after the 15th, three --repo scopes + S1; parse/grep-only, no cargo; the trigger: mmorpg's `c03337a` clippy heal (21:40) landed after the 15th run's window — the 13th-run lesson that inter-window landings escape detection; mechanical edits don't touch imports, but confirming is one command) | idle-loop unit (riir-clippy skill item 6; contract script `--repo riir-clippy` + `--repo riir-mmorpg-examples` + `--repo katgpt-rs` scoped + S1 mmorpg guard) | **CLEAN — exit 0 all four:** contract 10 edges (riir-clippy, incl. the Issue-058 `[patch]` table — the `katgpt-device-verify` allowlist row landed WITH the fix in `25f75f7`, the TAIL-P6 class NOT repeated), 28 edges (mmorpg, unchanged), 0 edges (katgpt-rs); S1 zero violations on the post-heal tree. Docs-only deltas since the 15th run (702-label fixes `7736e30`, rust.yml docs-drift job `1df3599`, mmorpg README plan-index `cd94bf9`) carry no dep-graph or code-logic surface. No findings, no issues filed. |
| 2026-09-01 (15th run — post-831/832/835/836/837-window delta check; contract script full-workspace + S1 mmorpg guard + S2/S4/S6/S7/S8 greps — parse/grep + compile-gated C6, no GPU; sibling trees verified first: riir-ai rematch/ 2-file WIP untouched (HEAD authoritative), riir-clippy Batch-94 WIP untouched) | idle-loop housekeeping unit (handoff decision-item 1) | **C6 FOUND RED + REPAIRED same session (riir-ai `2575b6519`) — then CLEAN: contract exit 0, 17 repos / 203 edges (up from 202; delta = this window's landings), SP1–SP5b + C0–C4 green; S1 zero violations; S2/S3/S4/S6/S7/S8 comments-only clean.** The C6 modelless-residue gate had been red since the Aug-30/31 landings (never re-run between): 3 keeper files +1 each from the B89/B90 heal `606ce69de` (compacted back to pin, zero semantic change — const/let blank line, buffer-op blank line, `count` inlined into its sole `with_capacity` consumer) + **bench_672 +132 REAL growth: Issue 781 `80b8eaad8` (2026-08-30) added `test_gpu_lora_multilayer_attach` (the L4 BCL4 G-decode prerequisite) and the ledger was never re-pinned at that landing — verified 1 test fn + zero training-shaped tokens in the added lines, re-pinned UP 218→350 with the reason on record (the honest up-pin: growth is committed bench content, not residue)** + 7 R3 shrunk rows re-pinned DOWN per the gate instruction. En-route RE-HIT of the documented TAIL P1 bash 3.2 gotcha: a lone apostrophe in a heredoc-parsed ledger row breaks the bash 3.2 quote tracker (`bash -n` fails at the case statement ~20 lines BELOW the row — the reported line is never the guilty line); annotation rephrased apostrophe-free, `bash -n` pinned green. Standing lesson for the next ledger editor: **the C6 row text must be apostrophe-free AND the real detector is `bash -n`, not running the guard** (the guard's own error points at the wrong line). Compile gate: `cargo check -p riir-engine -p riir-gpu --lib` clean. No issues filed (gate/ledger maintenance, not boundary code). |
| 2026-08-29 (14th run — post-Plan-556/087 delta check under the sibling's live 809/810 M3 bench) | idle-loop unit (riir-clippy skill item 6; full-workspace contract script + S1 mmorpg guard — parse/grep-only, no cargo, the Plan 337 discipline; sibling trees verified first: riir-ai carries the 65-file perf-league WIP with ZERO manifest/BOUNDARY.md surface (HEAD authoritative), riir-dapps the KAT Plan 013 WIP (src/kat + cloudflare/ + seam test, no manifest changes), mmorpg clean at `0a52b39`) | **CLEAN — exit 0 both: contract 17 repos / 202 edges all declared (up from 16/201 at the 12th run; delta = today's landings — Plan 556 Stage 3 riir-gpu internals via 4090-side commit `95b8f3e5c`, verified ancestor of origin/develop, + the 087 facade swap), SP1–SP5b + C6 green; S1 zero violations on the post-087 tree (`fa4063e` SDK-facade swap + `dcfc4df` lint heal scanned clean). No findings, no issues filed. Coordination note: the 4090 was probed for the armed riir-train 478-T3 window — reachable + compute-idle but YIELDED (sibling-owned: the Plan 556 lane landed on that box today + the armed b759/b772 watchers whose `GPU < 2 GiB` fire condition went live when the plan346 trainer exited; 478-T3 is a gradient-accuracy instrument, the Bench-649 contention-poisoned class) — recorded in riir-train 478 + the clippy queue snapshot.** |
| 2026-08-28 (13th run — per-repo guard S1 on riir-mmorpg-examples; idle-queue item 6 under a loaded box — grep-only, no cargo) | idle-loop unit (mmorpg tree verified CLEAN first incl. `66dd683` the 08-28 clippy heal; `scripts/ci_boundary_guard.sh` = pure grep, zero cargo refs) | **3 VIOLATIONS found — filed as riir-mmorpg-examples Issue 086 (`b2f46e4`), detection-only per discipline.** All in `src/authority/progress_anchor.rs` (landed `a9e9d00` 08-27 14:40, dapps Plan 006 T4/T5 — AFTER the 12th-run window; survived the 08-28 heal, mechanical edits don't touch imports): E2 ×2 (`riir_chain::domain_anchor_commit::AnchorSubmitQueue` in the production `flush` signature `:217` + a test `:368`) — root cause UPSTREAM in `riir-dapps/src/domain.rs:325` (`AnchorRound::submit_to` takes the chain-owned queue type as a parameter, forcing every consumer to name `riir_chain::`); fix route = dapps re-export + consumer re-point. E1 ×1 (`use riir_simloop::TierLoop` `:38`) — the sanctioned authority sim-loop pattern minus its EXEMPT_LEAKS row (landing session skipped the bookkeeping). Also noted: the per-repo guard is wired into NEITHER `ci_feature_guard.sh` nor workflows — silent-by-default detector (owner decision, not bundled). No fixes in the detection commit. **RESOLVED same day** (the 085 pattern): dapps re-export `AnchorSubmitQueue` at `riir_dapps::domain` (riir-dapps `6671eb5` — `AnchorRound::submit_to`'s signature had forced every consumer to name `riir_chain::`, the exact boundary the dApp layer exists to open) + the two consumer sites re-pointed (riir-mmorpg-examples `627800e`) + the missing `riir_simloop` EXEMPT_LEAKS row for the sanctioned authority TierLoop pattern (T3, same commit) — guard re-run 0 violations, validated under the exact feature sets. Issue 086 file removed per the noise-reduction rule; the fix commit message is the record. |
| 2026-09-02 (17th run — workspace contract gate `ci_boundary_contract.sh`; idle-queue item 6, zero-build run) | idle-loop unit after the Issue-844 armament fix (riir-ai `280ff51ec`) | **7 C7 violations on riir-armageddon only** (katgpt-core/dec/hla/micro-belief/personality/sense/types resolved TWICE: git@`6f392727` 243-stale + local path) — root cause traced: neuron-db's Plan-040 git-declared katgpt triple reaches the armageddon graph (armageddon-core → riir-games → riir-games-civ → riir-chain/neuron-db) and `[patch]` applies from the build ROOT only, so neuron-db's own patch table is inert there. Fix landed armageddon `c2e50fa` (build-root patch table for neuron-db's exact triple; lock regen → 0 git-sourced, 1 katgpt-core; drift row + issue 006 filed file-first). **OWNER VERDICT same day: riir-armageddon is OBSOLETE — repo skipped for all future work**; note: it stays in the gate's DERIVED repo set (root BOUNDARY.md + .git) until de-enrolled, an owner act. Everything else green: C6 modelless residue, C9 pin consistency, contract-rot 0. |
| 2026-08-28 (12th run — games-dep manifest audit + SP5 enforcement landed) | session unit (user-directed cross-repo audit; contract script full-workspace, 16 repos / 201 edges) | **CLEAN — exit 0.** Audit for the riir-train `0f472084` defect class (unconditional games dev-deps compiling ~116K LOC into default test builds) found ZERO remaining instances workspace-wide: chain (engine edges optional + engine-bridge workspace-excluded, Plan 040), neuron-db/dapps/dao/clippy/viewbridge/katgpt-rs all clean (dao deps = katgpt-core only, no riir-dapps dep by design — contract pins); riir-train fix verified holding (games deps `optional = true` in both consumer crates, cfg-gated modules, feature wiring intact incl. `civ_trajectory_collect` implicit activation). riir-dao drift ledger verified FIXED by sibling `4f24d8d` (D2 dispositioned by-design) — the 2 hard errors from the prior session's run are gone. **SP5 landed** (riir-ai `8da5e187e`): SP5a hard-zero repos (chain, neuron-db, clippy, dapps, dao, viewbridge, katgpt-rs) declare zero riir-games* deps in ANY deps section incl. workspace templates; SP5b riir-train keeps every games dep optional. Detector validated on 5 shapes (non-optional deps+dev-deps, optional single+multi-line, template inclusion/exclusion); `--repo` scoping verified both directions with ok-lines claiming only what was scanned. |
| 2026-08-27 (11th run — decision-item housekeeping; riir-train scoped re-check after the ninth-dispatch record) | dispatch idle unit (decision-item 1; contract script `--repo riir-train` scoped — parse-only, no cargo; shared checkout verified FIRST riding sibling `sync/p348-d-civ-ab-0826` now at `d48de0d8` — their branch moved +1 docs-only commit, the plan-357 filing; their 2 dirty gpu files + untracked plan 356 untouched) | **CLEAN on main — exit-1-on-disk is exactly the documented until-merge artifact (5th-run carryover, unchanged)**: the sole violation `riir-train → katgpt-device-verify` reads the sibling branch's stale BOUNDARY.md (diverged `930c210c`, merge-base predates the `9100cfe4` row landing); parser replay vs `git show origin/main:BOUNDARY.md` confirms the row present (line 35, D1/D2 rationale) → main CLEAN, self-resolves at sibling merge. Delta since the 10th run (`8d5ff609..origin/main@0d2dd064`): docs-only (`aba5c585` plan-358 filing + `0d2dd064` ninth-dispatch record — `git log -- Cargo.toml` confirms `8d5ff609` remains the newest manifest-touching commit), zero dep-graph surface; measured 22 edge rows, unchanged. No new findings, no issues filed, nothing fixed (touching the sibling branch forbidden). |
| 2026-08-27 (10th run — dispatch housekeeping; the riir-train scoped check the 8th/9th runs skipped) | dispatch idle unit (decision-item 1; contract script `--repo riir-train` scoped — parse-only, no cargo; the 9th run's parser-replay technique applied: shared checkout verified FIRST riding sibling `sync/p348-d-civ-ab-0826` at `3255954c` with their 3 dirty files untouched; sibling-branch manifests verified −54 lines features/`[[test]]`-only vs `origin/main`, ZERO `[dependencies]` changes → measured edge set identical) | **CLEAN on main — exit-1-on-disk is exactly the documented until-merge artifact (5th-run carryover, unchanged)**: the sole violation `riir-train → katgpt-device-verify` reads the sibling branch's stale BOUNDARY.md (diverged `930c210c` 2026-08-26 13:22, predates the `9100cfe4` row landing); parser replay vs `git show origin/main:BOUNDARY.md` confirms the row present (line 35, D1/D2 rationale) → main CLEAN, self-resolves at sibling merge. Delta since the 5th run (`be332d9c..origin/main` = 16 commits: 12 docs adjudications + 4 feat [343 T3.2 probe harness, 354 P4 T0/P2 T3/P2 T1]): all Cargo.toml deltas are features/`[[test]]` sections only (`speculative_decode`/`laprop_optimizer` forwards to the already-declared `riir-gpu` dep — no new edge); measured 22 edge rows / 21 distinct targets, all declared. No new findings, no issues filed, nothing fixed (touching the sibling branch forbidden). |
| 2026-08-27 (9th run — decision-item housekeeping; post-752/746/761-closeout riir-ai delta + katgpt-rs 692-promotion delta) | dispatch idle unit (decision-item 1; contract script `--repo riir-ai` + `--repo katgpt-rs` scoped — parse-only, no cargo; sibling trees verified first: riir-ai clean tree fast-forwarded to `2b89429db`, katgpt-rs clean tree fast-forwarded to `1cff4fa7`; riir-train deliberately skipped — shared checkout rides sibling `sync/p348-d-civ-ab-0826`) | **katgpt-rs CLEAN (0 edges — the `prover_selection` default promotion `1b65662f` is internal, no cross-repo surface); riir-ai CONTRACT ROT found + FIXED same session (`12284bf6c`)** — drift-ledger D4 (the 741 T4c/T4c2/T10 `pub`-widenings owner-call row) cited `.issues/741_riir_gpu_training_residue_stale_f4.md`, closed+removed at the 2026-08-27 741 close-out WITHOUT the row re-anchor (the row⟺open-issue rule; the TAIL-P6 class a FOURTH time). Disposition: the widenings are still in place + time-boxed-accepted → deleting the row would be dishonest; re-anchored **owner-call→by-design** citing the LIVE Proposal 041 §Phase 2 decision record (which already tracks the D4 cascade at its §session-3 L227 and owns the revisit+reverse via T2.5's decline-path) — the parser's sanctioned decision-record route, no duplicate-tracking issue filed. Post-fix `--repo riir-ai` exit 0 (73 edges). **En-route collision (Issue-665 class, yielded cleanly):** the 762 dual-allocation (`762_m3_gpu` vs `762_three_arm`, stale-highwater read in `2b89429db`) was found + fixed CONCURRENTLY by the sibling session (`7a7ea26d5`+`c304951a4` on origin: same 762→764 renumber, same highwater repair, same AGENTS.md row) — my identical renumber commit dropped at push-reject via `rebase --onto` (push-wins); only the disjoint D4 fix replayed through. Secondary rot noted NOT fixed (proposal-owner narrative): Proposal 041's header "Blocked by" still links the removed 741/744 issue files. |
| 2026-08-27 (8th run — dispatch housekeeping; post-043/e272a88/286d889/79933e9 delta + the dapps 002/003/005 closure catch-up) | dispatch idle unit (decision-item 2; contract script `--repo riir-chain` + `--repo riir-dapps` + `--repo riir-clippy` + `--repo riir-neuron-db` scoped — parse-only, no cargo; sibling trees verified first: riir-ai 5-file sibling WIP untouched, riir-train on sibling sync branch skipped per dispatch) | **chain/clippy/ndb CLEAN (11/8/3 edges); riir-dapps CONTRACT ROT found + FIXED same session (`f704a4d`)** — drift-ledger D1 cited `.issues/002`+`.issues/003`, both resolved+removed (`820d398`/`c96fd5f`) without the row removal (the row⟺open-issue rule broken; the closure commits removed the issue FILES but left AGENTS.md entries OPEN + the BOUNDARY.md row). Fix = the D1 row removed + AGENTS.md 002/003/005 OPEN→RESOLVED entries with landing commits (both "remaining" halves had landed: GM server surface via riir-ai `cd6b1b00a`+mmorpg `d48f37d`; CF warm-tier DO host via mmorpg `3dc9eb8`+ndb `79933e9`). Post-fix `--repo riir-dapps` exit 0 (4 edges). Direction gate OK. Doc-sync companion commits: ndb `6a33b62` (replay_wal_bytes doc coverage) + mmorpg `c5e72c6` (gm_server/warm-tier-do/item-NFT AGENTS.md section). Delta checked: chain Plan 043 token-registry flag (internal program feature, no dep change), clippy `286d889` plan356-A1 harness (8 edges unchanged), ndb `79933e9` (local_kv only). |
| 2026-08-27 (7th run — post-084-T2/dapps-002-T2/691-dumper delta check) | idle-loop unit (riir-clippy skill item 6; contract script `--repo riir-ai` + `--repo riir-mmorpg-examples` scoped — parse-only, no cargo, the Plan 337 discipline under the LIVE Issue 760 T1 baseline bench (PID 56490, GPU 99%, 75+ min) + the mmorpg-084 CCU soak in /tmp+ccu-wt; sibling trees verified first: riir-ai carries the 760 sibling's 2-file WIP untouched, katgpt-rs clean at `61a9c7e5`, mmorpg clean at `08ace34` — the soak sibling works in `~/git/ccu-wt` + `/tmp`, not the repo trees) | **CLEAN — exit 0 both scopes, zero findings, zero issues filed.** Delta since the 6th run = 3 riir-ai code commits + docs: (1) `cb89e6a1b` riir-net fan-out counters + traced dual-listen (hub.rs/relay.rs — transport-only; the "avatar" grep hits are the relay's payload-semantics docs, correct vocabulary for what the relay carries); (2) `cd6b1b00a` mcp-client GM control-plane SERVER half (new `server` feature forwarding already-optional `dep:wtransport`/`dep:tokio` + tokio `rt-multi-thread`/`time` — crates.io-only, zero cross-repo deps; the "quest" vocab hits are `request`*` substrings + tier-health REPORT semantics in prose, no game logic); (3) `b7c99a7da` SRTR trace dumper (riir-engine example, own-crate imports only, feature-gated `deltanet_ternary_inference`). **The one real edge change — mmorpg → riir-mcp-client promoted from dev-only to runtime (default-off `gm_server`) — is properly rowed**: BOUNDARY.md row 55 documents the promotion + Issue 002 citation + the dev-dep parity-pin rationale; `--repo riir-mmorpg-examples` run confirms 28 edges all declared. katgpt-rs delta since the 6th run: docs-only (001/691/692 records, research 509). riir-ai scoped: 73 edges, SP1-SP4 + C6 all green (pre-existing SP2a by-design notes + R3 WARN unchanged). |
| 2026-08-26 (6th run — post-Plans-575/576/577 check over `b9fa4a07`+`6dcce272`+`06e8a7dd`) | handoff unit (decision-item 1; contract script `--repo katgpt-rs` scoped — parse-only, no cargo; katgpt-rs tree verified first: HEAD `06e8a7dd` == origin/develop, sole untracked = the sibling's issue-691 file, left alone — the same 14-file WIP the 5th run saw is now COMMITTED as these three commits + the sibling's `43f0cacf`/`0489787d`/`eca969bb` interleaves) | **CLEAN — exit 0, zero findings.** The delta = the Plans 575/576/577 execution: `crates/katgpt-core` Cargo.toml feature wiring ONLY (`risk_control_exit = []`, `distributional_steering = []` + `[[test]]`/`[[example]]` targets — the `stale_residual` block in range is the sibling's `43f0cacf`), zero new deps; katgpt-rs measured **0 cross-repo dep edges** (upstream, as designed). `riir-` grep on the two new sources + lib.rs: comment-only prose, zero dep lines. Game-vocab grep (`quest|monster|avatar|npc|pet|hero|taming|orchard|fear|karma|fame`) on both new primitives: **0 hits** — both are pure modelless math (UCB/Hoeffding calibration; FK weights + Picard), passing the katgpt-rs domain test (modelless inference primitive, no riir dep). Feature-flag discipline held: both stay opt-in behind GOAT'd gates (Bench 681 PASS modelless / 682 FAIL-partial honest negative). Floor-rule note (not a boundary finding): Bench 681's UQ-bearing instantiation is the naive-calibration contrast (G1: naive 7/40 violations vs UCB 0/40) + the dual-vs-single-vs-fixed exit floor (G2) — the literal `ConformalIntervalCalibrator<SeasonalNaiveForecaster>` CRPS/Winkler floor is undefined for a decision rule; if the owner wants the literal floor at re-gate, that is a gate-scope call, not a contract violation. |
| 2026-08-26 (5th run — arm_c-liveness-gated dispatch, scoped delta check `95ea624d..be332d9c`) | idle-loop unit (decision-item "arm_c alive → boundary-guard + doc-sync, stop cleanly"; contract script `--repo riir-train` scoped — parse-only, no cargo, the Plan 337 discipline under the STILL-RUNNING p335 arm at PID 94779, 7h48m elapsed, ~30.6% MEM; sibling trees checked first: riir-train shared checkout on sibling `sync/p348-d-civ-ab-0826` with their 2 dirty gpu files untouched, katgpt-rs 14-file sibling WIP on plans 575/577 — disjoint from this skill's files, riir-ai 5, mmorpg 1) | **CLEAN on the delta — main CLEAN; the 1 disk violation is the documented until-merge artifact (4th-run carryover, unchanged).** Delta `95ea624d..be332d9c` = 354-T3/T4 (optimizer_laprop_gpu/optimizer_select/dflash_trainer — all internal `crates/riir-train-gpu` modules + tests, ZERO Cargo.toml dep changes; Cargo.lock −1 line) + 346-A4/483-T3 code + docs cluster → dep-graph surface structurally unchanged (21 edges). Scoped contract run: true exit 1 from exactly the known artifact — `riir-train → katgpt-device-verify` read from the sibling branch's stale BOUNDARY.md (merge-base confirmed the branch predates `9100cfe4`); parser replay via `git show origin/main:BOUNDARY.md` line 35 shows the row present with the D1/D2 rationale → **main CLEAN**. Self-resolves when the sibling merges main. No new findings, no issues filed, no fixes (touching the sibling branch is forbidden). |
| 2026-08-26 (4th run — decision-item scoped check over `95ea624d`) | idle-loop unit (riir-clippy skill item 6; contract script `--repo riir-train` scoped — parse-only, no cargo, the Plan 337 discipline under the STILL-RUNNING p335 arm at PID 94779, 2h10m elapsed, ~20.8 GB RSS; sibling trees checked for WIP first: riir-train shared checkout on sibling `sync/p348-d-civ-ab-0826` with their 2 dirty files untouched, katgpt-rs clean) | **CLEAN on the delta — docs-only commit, zero boundary surface.** Checked commit `95ea624d` (riir-train origin/main tip, the previous session's Issue 469 T2.1 landing): 4 files, all `.docs/`+`.issues/` markdown, no Cargo.toml/src → no dep-graph and no code-logic surface. Scoped contract run reproduces exactly the 1 documented until-merge artifact on disk (`riir-train → katgpt-device-verify` absent from the sibling branch's stale BOUNDARY.md); parser replay against `origin/main:BOUNDARY.md` confirms the row present (line 35) → **main CLEAN**. Delta since the 3rd run: `1f689c8b`+`0020e3f1`+`95ea624d`, all docs-only. No contract rot, no new findings, no issues filed. |
| 2026-08-26 (3rd routine run, post-yield verification) | idle-loop unit (riir-clippy skill item 6; contract script full-workspace + S1 mmorpg guard + S2/S4 delta greps — parse/grep-only, no cargo, the Plan 337 discipline under the p335 T4.2f measurement at load ~10.9; sibling trees checked for WIP first: mmorpg + game-sdk + katgpt-rs clean, riir-train shared checkout untouched) | **CONTRACT: main CLEAN — the 1 on-disk violation is the documented until-merge artifact, now verified WITHOUT touching the checkout via parser replay** (new technique for stale shared checkouts: run the script's own `allowlist()` awk against `git show origin/main:BOUNDARY.md` — it extracts `katgpt-device-verify`, so C3 passes at main; the disk read fails only because sibling `sync/p348-d-civ-ab-0826` predates `9100cfe4`). C6 clean (the recorded R3 re-pin WARN stands). S1 (mmorpg `91b33e5`, clean tree): **4 violations → Issue 085 filed `e7b06c7`, detection-only — then RESOLVED SAME DAY, all 4.** Class A E2 ×2 (`pet_teaching.rs:123/:171` direct `katgpt_core::hint_regret` from the Plan 576 migration `bae567c` 2026-08-24 — the consumption substrate-orthodox + bit-identity-pinned, the ROUTE was the violation) fixed via the SDK `hint_regret` facade forward (game-sdk `0a65703`, the `spectral_hero_gate` pattern: opt-in `["dep:katgpt-core", "katgpt-core/hint_regret"]` + thin `riir_game_sdk::hint_regret` re-export, `demo_coverage_curiosity` composes it) + the consumer swap + direct-dep removal (mmorpg `baab368` — the root `[dependencies]` katgpt-core entry deleted; graph unchanged transitively). Class B D2 ×2 comment-text FPs fixed in the guard itself (mmorpg `0aba62b`: full-line-comment stripping in the Phase-2 snippet + `#[test]`-fn body skip in the Phase-1 awk, 7-control fixture validation — real D2/D1 code hits still fire, no stale-marker suppression, production fns after test fns still scanned). **Guard final state: exit 0, ZERO violations — first green S1 since 2026-08-22.** pet_teaching gates 15/0/3i through the facade (g677 A/B + determinism + G4 alloc). Issue 085 removed per the noise-reduction rule (records: the fix commits + this row). Both classes had landed in the S1 blind window since 2026-08-22 — the 08-26 contract-only runs are dep-graph-level and structurally cannot see code-route leaks. S2 (SDK heavy deps all `optional = true`) + S4 (games-shared `dep:katgpt-core` gating) re-verified holding on the delta. |
| 2026-08-26 (later session — decision-item housekeeping) | idle-loop housekeeping unit (scoped riir-train run + full-workspace context; parse-only, no cargo; trees verified for sibling WIP first — riir-train shared checkout on sibling sync branch with 2 dirty files, worked via main-worktree) | **4 violations, all one class — DISPOSITION RESOLVED, 3+1 FIXED same session.** All 4 = `katgpt-device-verify` patch-section entries without BOUNDARY.md rows (riir-ai, riir-game-sdk, riir-mmorpg-examples ×2 manifests, riir-train). The 08-26-morning "owner arbitration pending" stance is OBSOLETE: riir-ai Issue 759 CLOSED with D1 executed — the patch lines are now the sanctioned mechanism (D1 = cover the crate; D2 = the lock-churn hygiene rule), landed as riir-train `d6ad11da` + game-sdk `4b23dce` + the riir-ai/mmorpg lines — but WITHOUT the contract rows (the Issue 741 TAIL P6 class a THIRD time). Fix = allowlist rows citing the closed arbitration (the `katgpt-attn-match` precedent): riir-ai `538a7aad0`, game-sdk `7f3b6aa`, mmorpg `91b33e5` (direct develop commits, clean trees) + riir-train `9100cfe4` (main via worktree — the shared checkout rides sibling `sync/p348-d-civ-ab-0826` which hasn't merged main yet, so the violation stays VISIBLE-until-merge, expected self-resolve; re-run shows exactly that 1 remaining). No drift rows, no new issues — the row IS the contract catching up with a closed decision. |
| 2026-08-26 (idle-loop housekeeping, post-Plan-348-F-row-2 session) | idle-loop housekeeping unit (riir-train session dir scoped + full workspace contract run — parse-only, no cargo; verified all trees for sibling WIP first: riir-train 3-file sibling WIP untouched) | **SCOPED (riir-train): CLEAN — exit 0** (21 edges, all declared, ledger valid, HEAD `c08fbcbc` == origin/main). **FULL WORKSPACE: 2 violations — both the katgpt-device-verify `[patch."https://github.com/katopz/katgpt-rs"]` line landing (2026-08-24: riir-ai `d039a9164`, mmorpg `f0cbc13` + `wasm/Cargo.toml`) WITHOUT the BOUNDARY.md 'May depend on' row — the Issue 741 TAIL P6 class REPEATED one day after that fix (katgpt-attn-match, 2026-08-23). REPORTED, NOT FIXED — cause established:** the patch lines' disposition is OPEN OWNER ARBITRATION in riir-ai Issue 759 (D1–D4 + the 108/606 threads own the dead-line removal) — adding allowlist rows now would bless lines whose fate is undecided; riir-chain + riir-neuron-db DO declare their (real-dep) rows. Both repos' trees were clean at check time. Note: the 2026-08-22 full run predates the 08-24 landings; the 08-24 run was riir-train-scoped and never saw them. |
| 2026-08-24 (scoped routine check) | idle-loop housekeeping unit (riir-train only; contract script `--repo riir-train` during two live sibling sessions — 4090 Plan-342 cluster + M3 Plan-346/riir-game-sdk builds — no cargo invocations, the Plan 337 discipline) | **CLEAN — exit 0.** 21 cross-repo dep edges measured, all declared; ledger valid. riir-train is contract-gated only (not a surface-1 code-logic repo). Run against HEAD `fa46f627` (M3 == origin/main); the M3 working tree carries documented stale sync residue (deleted-at-HEAD Bench 510, modified plan-342/bench-508/research-423, untracked stale 479 draft) — deliberately untouched, HEAD authoritative. Delta since the 2026-08-22 run touching riir-train: only `77d23662` (docs) + the Plan 342/343/346 feat cluster (no Cargo.toml dep changes — the measured 21-edge set is unchanged). |
| 2026-08-22 (routine check) | idle-queue routine check (riir-clippy skill item 6; grep-only + both scripts during the sibling riir-train 472 T6 auction-CDF measurement — no builds, the Plan 337 discipline) | **CONTRACT CLEAN (15 repos, 177 edges — up from 173 at the 2026-08-21 first full run); S1: 4 violations → Issue 081 filed (`8c30e93`), detection-only.** All 4 are E1/E2 facade leaks in `src/asset_vessel.rs` (`:89`/`:91` production `riir_neuron_db::load_vessel`/`decode_art_bundle`; `:132`/`:204` test-mod imports) — introduced `db20ebd` 2026-08-21 22:30 (Issue 735 T4/Plan 543), i.e. AFTER the 2026-08-21 routine run and under its contract-only check (Plan 543's "boundary-checked first" ran `ci_boundary_contract.sh`, which is dep-graph-level and stays clean — the dep is feature-gated; the code-logic guard never saw the landing). The asymmetry that makes it real: consumer `static_data`/`warm_tier` both forward through SDK impl gates while `asset_vessel` deps riir-neuron-db directly — the SDK `vessel_impl` carries `neuron_vessel`, not the `art_vessel` family. Fix route (Issue 061 precedent) recorded in 081. **RESOLVED same day** once the sibling RLVR run freed the box: SDK `art_vessel_impl` facade gate + `riir_game_sdk::art_vessel` module (riir-game-sdk `1a6fe62`) and the consumer swap to `asset_vessel = ["riir-game-sdk/art_vessel_impl"]` with all 4 import sites on the facade path (riir-mmorpg-examples `71d3c10`, Issue 081 removed per noise-reduction). The FACADE route was chosen over the `EXEMPT_LEAKS` sanction route precisely because of the static_data/warm_tier asymmetry above. Re-verified: guard exit 0, contract still 177 edges (a facade swap moves the edge consumer->SDK, it does not add one), the 5 boot tests byte-identical. Note `art_vessel_impl` is a SIBLING of `vessel_impl`, not a subset — one forwards `neuron_vessel`, the other `art_vessel`; a future guard finding on either vessel family should check WHICH family before assuming the existing gate covers it. **S2–S7 CLEAN on the delta** since 2026-08-21: S2/S3 SDK root — all heavy deps still `optional = true`, the new `spectral_hero_gate` (`7a25c11`, riir-ai 736 B2) is the sanctioned opt-in forward of `katgpt-core/spectral_pencil`; S4 no riir-games-shared dep changes; S5/S6 no unity/viewbridge changes; S7 no riir-clippy dep changes. Delta scanned: mmorpg `00fa172`+`b659389`+`00ab1be`, riir-ai `5aecd37bd` (riir-net manifest module — unconditional riir-net-internal, not a boundary surface), game-sdk `a491aae`/`7a25c11`. |
| 2026-08-21 (routine check) | idle-queue routine check (riir-clippy skill item 6; grep-only S1 script during the sibling bonsai L4 RLVR training run — no builds, the Plan 337 discipline) | **CLEAN — exit 0, zero violations** on the tree that includes the sibling's in-flight tier-health WIP (uncommitted `src/tier_health.rs` + `Cargo.toml`/`Cargo.lock`/`src/lib.rs`/`sim_loop.rs` mods — the riir-dapps Issue 001 T4 producer). The new module (authority-side `TierHealthSnapshot` producer, gated `not(wasm32)` + `warm_tier`) introduces no facade leak, no game-logic drift, no geometry duplication; the `riir-mcp-client` reach is a dev-dep parity pin, not a production import, so Check E is correctly silent. Post-079 extraction tree (P0a–P8 landed) re-verified holding — the surface stays green since the 2026-08-19 Issue 075 fix. |
| 2026-08-21 | **first full contract run** (Issue 737 T-RUN, via the new `ci_boundary_contract.sh`; closes the 724+737 pair) | **1 real code leak + 5 contract bugs — all fixed; gate now green (15 repos, 173 edges, 0 violations).** The code leak: riir-chain's ROOT `riir-wasm` dep had no `default-features = false` while riir-chaind's copy did — so riir-wasm's default (leap_strategy, skill_lifecycle) still activated riir-engine features for the whole chain graph (riir-chain `f19c6ee5`: engine features on the `chain_guard` path **1 → 0**, packages **106 → 104**; check + clippy clean at `chain_guard` and default). The contract bugs: (1) the hub CANONICAL matrix's `riir-neuron-db` row was measured **backwards** — the arrow is `riir-engine → riir-neuron-db` and neuron-db declares ZERO riir-ai deps; (2) three more matrix rows listed crates their repo does not use; (3) ~45 real cross-repo edges were missing from the per-repo allowlists (katgpt leaf crates, `neuron-db-sdk`, the sanctioned game→chain + game→dapps edges, viewbridge-node's adapter deps); (4) **four repos had no BOUNDARY.md at all** — `riir-auth` (3 live edges), `riir-armageddon` (2 live edges: `riir-games` + `katgpt-core`, entirely unaudited), `riir-burner`, `katgpt-web`: the workspace is **15** repos, not 11. Only `riir-auth` fell out of the first run, and by accident (it appeared as a dep OWNER in the edge dump); the other three needed check **C0**, which derives membership from OUTSIDE the set — C0a from the measured edges, C0b from the `riir-*`/`katgpt-*` naming convention (third-party clones under the same parent are excluded by it, and git worktrees are skipped by requiring `.git` to be a directory). **A set that enumerates its own members cannot notice a missing one** — the same failure shape as the inverted matrix row below; (5) split-prep invariant 2 ("never a games dep or default game feature") was FALSE as written — `riir-ffi/game_ai` is default-on by design — now split into 2a (games deps optional in chain-facing crates) + 2b (non-game consumers pass `default-features = false`), and 2b is what caught the leak. Methodology lesson: **a prose contract cannot catch an inverted measurement.** Every row that reads like a fact ("repo X may depend on Y") is a measurement with a direction, and the direction is exactly what prose review gets wrong. |
| 2026-08-19 | idle-queue routine check (riir-clippy skill item 6; grep-only + S1 script during the sibling p335 LoRA measurement — no builds, the Plan 337 discipline) | **1 violation — S1 Check D1, NEW since 2026-08-17**: `src/remote_smoothing.rs` (guard cites fn `:218 path_at`; math sites `:237` inline `(dx*dx+dy*dy).sqrt()` + a hand-rolled `dist_sq` family at `:362/:366/:458/:527/:697` + `:404`) — landed `7ee3104` 2026-08-18 (Plan 025 remote movement smoothing). **Filed as mmorpg-examples Issue 075 (`4961be2`), detection-only** — scope nuance recorded there: the file is client-view interpolation/dead-reckoning over `[f32;2]` wire positions, so substrate-delegation vs guard-exemption is the owner's call (the `shared_world.rs` EXEMPT precedent). **S2–S7 all CLEAN** — S5 post-wire-deletion surface verified for the first time: user C# is now only OrbitBenchmark + SceneSetupMenu + TutorialInfo boilerplate; the 3 viewbridge adapter templates (`RiirGameNode`/`RiirViewBridge`/`TransformConsumer`) grep clean for game logic + AI verbs; S6 latent grep hits only the guard's own doc-comments, `TransformView` still raw-only (`pos[3]`+pad+`rot[4]`); S4 Issue 682 `optional = true` + `dep:` gating intact; S2/S3 SDK deps all optional or sanctioned (riir-games-shared + riir-net always-on facades). |
| 2026-08-17 (evening, 2nd run) | idle-queue routine check (riir-clippy skill item 6; S1 script run in a clean temp worktree at origin/develop `222519c` — the main checkout holds sibling WIP) | **2 violations — both Check D1** in `src/issue068_measure.rs` (`radius_stats` :46-48 + probe `a_authority_spread` :134-136; guard cites the fn lines :35/:102): inline `(dx*dx+dy*dy).sqrt()` duplicating `MapPos3D::distance_2d_to` — Issue 068's diagnostic probes, landed after the earlier green run. **Filed as mmorpg-examples Issue 069 (`a48af77`), detection-only** per discipline: fix must be verified bit-identical against the recorded 068 probe numbers, and 068 is owner-decision-pending. Context finding recorded: the guard is still manual-only in this repo (no CI workflows) — wiring it in is a separate decision, not tasked.
| 2026-08-17 (evening delta) | idle-queue (riir-clippy skill item 6; grep-only during the sibling bonsai GPU run — no builds) | **CLEAN, no findings.** Delta since `59e3113d9`: riir-clippy `7ec8f24` (T4.4 L4 e2e harness — grep-verified zero game/engine/chain refs, std + crate-local only), riir-ai Plan 529 Phases 4–7 + T5.8 (`riir-agents` — not a boundary surface; the only dep change is optional `arc-swap` from T2.7), riir-train 336/455/454 (not surfaces). S4 re-verified holding (Issue 682 `optional = true` + `dep:` gating intact in riir-games-shared). S7 re-verified (zero game deps in riir-clippy). S1/S2/S3/S5/S6 = M3-side surfaces (riir-mmorpg-examples / riir-game-sdk / riir-unity / riir-viewbridge not present on the 4090 box; last full run today green per the log above). |
| 2026-08-17 (targeted delta check) | idle-queue (riir-clippy skill item 6; grep-only during the t42b measurement window — no builds) | **CLEAN, no findings.** Delta since the morning S1 runs: two cross-boundary landings checked. (1) **S7** riir-clippy `ab2d726` DaemonL4 client (the first real L4 transport, landed 09:12) — std-only verified (`std::io`/`std::process` + `crate::l4_fallback` only; zero riir-games/engine/sdk/chain/gpu refs) — the dev-tool no-game-coupling surface holds as L4 goes from deferred-seam to live-transport. (2) **S5** riir-unity OrbitBenchmark `3c759c4` (P8 measurement ring) — measurement-only C# (frame-time ring, percentiles, bridge-driven publish→consume→apply cycles), no game vocabulary, no engine recompute. riir-unity as a whole deferred to the ACTIVE sibling substrate-first audit (their Issue 003, wire-mirror drift pinning, is the live finding there — do not double-audit). |
| 2026-08-17 (post-fix + sibling-WIP check) | idle-queue routine check (riir-clippy skill item 6; S1 script run, repo = riir-mmorpg-examples) | **GREEN — exit 0, zero violations** on the tree that includes ACTIVE Plan 022 sibling WIP (11 modified + 5 untracked). Confirms the Issue 061 fix run held (guard 2 → 0: pet_ai.rs substrate `position_toward` swap + shared_world.rs EXEMPT_LEAKS sanction) AND that the in-flight Plan 022 phases introduce no new facade leaks. No findings, no issues filed. |
| 2026-08-17 (guard-semantics change) | Issue 061 task 4 (riir-clippy idle loop; Check D test-awareness) | **Detection-semantics fix, not a code fix** — `ci_boundary_guard.sh` Check D now skips `#[cfg(test)] mod X { ... }` blocks (mmorpg-examples `ca50093`): resolves the static_data.rs:1291 test-only FP (independent closest-AABB-point geometry — substrate-first exempts test code). Scope: inline mod form only (external `mod tests;` NOT tracked — production code after it stays scanned); `cfg(all(test, ...))` covered; Check D only (A-C + E unchanged, documented in E's comment). Validated: 9-fn fixture (prod fns before/after mods still scanned, both mod forms exempt, external no-over-skip) + full-src A/B — **221 test fns exempted, 0 production fns lost** (emitted-fn-set diff empty); pet_ai Check A + shared_world E2 verified still firing. Guard 3 → 2. **Future runs: the 221-fn reduction in Check D's scanned surface is by design — not silent violation decay.** |
| 2026-08-17 (fix run) | Issue 061 task 1 (riir-clippy idle loop; post-fix re-run + enumeration correction) | **5 of 8 sites FIXED** — SDK `warm_tier_impl` now re-exports `karma_history` types (riir-game-sdk `94f3198`), consumer sites swapped (mmorpg-examples `70a5aac`); guard 8→3. **Enumeration correction:** the 2026-08-17 detection entry's "8 = 6 unique + dedup" was wrong — output truncation hid 2 sites: pet_ai.rs:237 (Check A — Phase G pet-alarm intercept hand-rolls `hero + (h−hero)/\|h−hero\|·R` beside a `follow_hero` substrate call; fix = substrate `position_toward` extension in riir-games::swarm::pet, tasked in Issue 061) + static_data.rs:1291 (Check D test-only FP — independent closest-AABB-point geometry, substrate-first exempts test code; fix = guard Check D mod-tests awareness, tasked). Remaining 3: those 2 + shared_world.rs:73 (sibling WIP, deferred to Plan 022 owner). Lesson: the guard's ~40-line-per-violation output truncates in `tail` views — capture the FULL violation list when filing. |
| 2026-08-17 | idle-queue routine check (riir-clippy skill item 6; S1 script run, repo = riir-mmorpg-examples) | **1 actionable finding — S1 `ci_boundary_guard.sh` exit 1: 8 E1/E2 facade leaks, all new since the 2026-08-16 clean run, from Plan 022 phases F/H/K.** 5 sites landed (karma.rs:48/791 + warm_tier.rs:480/490/491 — `riir_neuron_db::karma_history` types reaching past the facade), 1 site in ACTIVE sibling WIP (shared_world.rs:73 `riir_simloop::SimLoop` — uncommitted). Detection-only per discipline: filed as riir-mmorpg-examples Issue 061 (commit `f3ac749`), fix route = SDK `karma_history` re-export (Issue 053 Part 2 precedent) + a `riir_simloop` facade-or-sanction decision; shared_world site deferred to the Plan 022 owner (do not touch sibling WIP). No fixes in the detection commit. |
| 2026-08-15 | boundary-guard quarterly gate (agent session, katgpt-rs handoff) | 6/7 surfaces CLEAN; 1 actionable finding — Surface 4 half-gated dep: `katgpt-core` non-optional in riir-games-shared while its only consumers (`grudge_field`, `sleep_time_reload`) are cfg-gated (Issue 682). **Fixed same session** (commit `e982ef7d2`, riir-ai develop): dep optional + `dep:katgpt-core` in both features + implicit `katgpt-core/sleep_time_anticipation` need made explicit; no-features check 15.45s→5.76s, zero katgpt-core artifacts; all feature paths/forwarders/wasm32/reverse-deps/clippy/514 lib tests green; workspace Cargo.lock unchanged. Bonus pre-existing fix: stats doctest used the pre-move `riir_games::` path (commit `20abe832d` — no pinned suite runs `-p riir-games-shared --doc`, the "which gate runs this?" blind spot). Surfaces verified clean: 1 (`ci_boundary_guard.sh` exit 0), 2/3 (all heavy SDK deps optional + feature-gated + documented), 5 (WireProtocol.cs = documented open-issue debt, not re-filed), 6 (raw-physical only), 7 (opt-in arms documented, default = katgpt-core only). Skill checks refined this run: S2/S4/S7 grep notes now encode the sanctioned exceptions + the half-gated rule. |
| 2026-08-15 (evening) | idle-queue routine check (riir-clippy skill item 6, Issue 019 T8; grep-only run during the Plan 337 32K measurement — no builds) | **7/7 surfaces CLEAN, zero violations, zero new issues.** Delta since the morning quarterly gate: only sibling WIP (riir-ai qv_lora/riir-gpu) + riir-clippy fix_verify WIP — none touch a boundary surface. S1 `ci_boundary_guard.sh` exit 0. S2/S3 SDK root: heavy deps all `optional = true` (comments-only grep hits). S4 Issue 682 fix holding (`dep:katgpt-core`/`dep:riir-engine` gating intact). S5 user C# zero sigmoid/dot_product/class-System hits (PackageCache hits excluded as toolchain, not user code; WireProtocol.cs remains documented open debt = riir-unity Issue 002 Phase A, user-gated). S6 viewbridge: only the guard's own doc-comments. S7 riir-clippy: no game deps; engine/gpu/rag `optional = true` behind features (sibling Cargo.toml WIP does not add coupling). First idle-queue invocation of the check — T8 closes on this run + the morning gate. |
| 2026-08-16 | idle-queue routine check (riir-clippy skill item 6; grep-only + S1 script run) | **7/7 surfaces CLEAN, zero violations, zero new issues.** Delta since 2026-08-15 evening: Issue 599 derive fix (neuron-db-derive attr parser + mmorpg SpawnZoneRow attribute trim — no boundary surface), Issue 430 G2/G3 validation (riir-train bench example — not a surface), sibling riir-ai GPU WIP (705/706 — not surfaces). S1 `ci_boundary_guard.sh` exit 0. S2/S3 comments-only hits (all heavy deps optional). S4 gating intact. S5 zero user-C# hits. S6 clean. S7 clean. |
